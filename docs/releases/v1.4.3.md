# ArcWriter 1.4.3

发布日期：2026-08-11

## 版本重点

- 修复 AI 文件规划中不适用字段为 `null` 时导致整份操作计划失败的问题；操作类型和文件路径仍保持严格校验。
- 拆书批处理与分层归并使用专用 JSON 提示词，不再和设定提取的 Markdown 模板冲突；模型缺少部分分类时保留有效信息并补齐空类别。
- 完成仓库整理：历史压缩为 `v0.4.0` 与 `v1.4.3` 两个版本节点，GitHub 只保留两个对应 Release。
- 小范围正式发布成为默认流程：匹配桌面版本的带注释 `vX.Y.Z` 标签会完成验收、打包并创建 GitHub Release。

## 发布内容

发布附件仅包含 `ArcWriter-Setup-1.4.3.exe`、对应 `.blockmap` 与 `latest.yml`。

## 发布定位

这是免费、小范围正式发布，适用于小规模用户使用。安装包可能未签名；本版本不声明商业稳定性、严格 RC、密封评估、长时间 soak 或商业级性能验证已经完成。
