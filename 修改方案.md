# ArcWriter 本轮问题修改方案（含全局 AI 写入链路审计）

> 状态：已按本方案完成业务修改；自动化测试、桌面冒烟与端到端验收通过，待目标分辨率人工验收
> 范围：故事大纲 UI 与删除、新建项目落点、模型选择入口、思考模式与思考内容、项目上下文读取、AI 助手流式输出、全软件 AI 生成预览/确认/真实写入、正文侧栏独立迷你对话与段落扩写、拆书入库状态流、Markdown 拆书报告、融梗/蒸馏入口与蒸馏原文注入
> 产品原则：正文与创作内容优先；AI 使用的上下文可见、可追溯、可调整；高风险写入必须预览确认。

## 1. 方案结论

本轮不是从零补功能。代码中已经存在模型发现、会话级模型覆盖、思考等级参数、项目上下文拼装、NDJSON 流式传输、前端增量渲染、生成缓存和资料库草稿，但这些能力之间还没有形成一致的产品闭环。新增截图暴露的保存故障属于 P0：界面没有呈现真实待确认事务，用户输入“确认”后只是继续与模型聊天，模型可以口头宣称“已保存”，实际写入接口却没有执行。

| 需求 | 当前结论 | 修改重点 |
| --- | --- | --- |
| 红圈大纲区排版与编辑 | 中间行的 CSS 网格列数与真实 DOM 不匹配，导致文字被压成竖排；编辑能力只在右栏 | 重做情节点行布局，中间区支持就地编辑，右栏改为高级信息与检查结果 |
| 删除故事情节点 | 页面只有新增、编辑、上移和下移，没有删除处理函数与入口 | 增加带影响说明的行内删除；正确处理子节点、选择状态、并发冲突和撤销 |
| 新项目默认进入 AI 助手 | 创建成功只刷新项目并关闭创建面板，没有路由跳转 | 仅在“新建成功”后进入 /assistant，覆盖直接创建和二次确认创建 |
| 设置页移除模型选择 | 手动模式和网站模式都仍有语言模型选择；助手内已有模型入口，但只保存会话覆盖 | 设置页移除文本语言模型/副模型字段；助手最近一次显式选择同时保存为全局默认 |
| 思考模式开关与内容 | 当前只能选低/中/高，没有开关；响应解析器只读取正文 content/text | 增加明确开关，并建立独立的思考增量通道、存储字段和可折叠思考面板 |
| 核查大纲/设定参考链路 | 链路仍在，但结构化故事大纲与稳定上下文读取的文件名存在断层 | 统一项目上下文来源，优先读取当前结构化投影，并补端到端回归测试 |
| AI 助手流式输出 | Provider SSE → Runtime → NDJSON → API Client → React 的正文流已存在 | 保留并加固正文流；新增思考流、首字延迟/中断/无思考内容等状态 |
| 预览确认与真实保存 | 新助手只识别一种 pending_save；通用确认、资料草稿与文件操作确认没有统一呈现 | 建立统一待写入事务卡，按钮调用真实 API，保存后读回校验并刷新对应页面 |
| 风格/题材/设定同类问题 | 后端已生成待确认资料草稿，但顶层确认状态被覆盖，且新版产品页没有草稿审核入口 | 修复响应状态透传，在助手与目标资料页同时提供预览、确认和丢弃入口 |
| 全软件同类排查 | 各功能使用生成缓存、资料草稿、Agent confirmation、批次草稿等多套协议 | 统一状态契约和系统保存回执，并建立按写入域覆盖的验收矩阵 |
| 正文侧栏小型 AI 对话 | 当前侧栏复用全局会话，只展示最后一条 AI 回复；输入和发送也会影响完整 AI 助手 | 改为独立新会话的迷你对话框，在侧栏内显示双方消息和流式回复，不自动跳走 |
| 扩写当前段落 | 当前只有续写、检查人物口吻、增强悬念三个快捷操作，没有稳定的当前段落定位 | 保留原三个快捷操作及行为，新增“扩写当前段落”，优先使用选区，否则取光标所在段落 |
| 拆书流程与书库 | 导入即创建正式书库目录；对已导入书再次拆解会新建第二本，状态仅靠“是否存在任意产物”猜测 | 建立明确状态机，导入进入待拆队列，原书原地拆解，完整成功后才进入可操作的拆书库 |
| 拆书 Markdown 模板 | 当前分散生成设定提取和反向细纲 TXT，没有统一、可阅读的拆书报告 | 参照指定 Notion 笔记结构，每本书生成一份规范 `拆书报告.md`，同时保留机器可读 manifest |
| 融梗/蒸馏与原文注入 | 融梗按任意拆解文件判定完成；蒸馏可退回当前文档/拆解产物，后续创作只注入蒸馏档案，不注入原文证据 | 只允许完整拆解书籍进入动作区；蒸馏建立时和启用后创作时都按预算注入可追溯的原文片段 |
| 融梗/蒸馏成果落库 | 融梗只写独立候选目录，不进大纲；蒸馏只写隐藏 JSON，不进普通风格库，启用后还会替代普通风格上下文 | 候选与正式资料分层；用户确认后分别通过 StoryPlanningService / ProjectLibraryService 写入大纲与风格库 |

## 2. 已确认的产品口径

1. 模型选择只出现在 AI 助手的模型弹层中。
2. 用户在 AI 助手中最近一次显式选择的文本模型，同时成为全局默认模型；大纲完善、批量生成、审阅等其他 AI 功能跟随该默认值。
3. “模型选择”在本方案中特指文本语言模型。Embedding 模型与封面生图模型服务于不同能力，仍保留在对应的检索/封面设置中。
4. 思考过程只显示上游模型真实返回的思考字段，不通过提示词要求模型伪造，也不把普通正文误标成思考内容。模型或中转服务不返回思考字段时，明确显示“该模型未返回可展示的思考过程”。
5. 大纲中间区采用“点击进入就地编辑 + 明确保存/取消”的方式，不使用难以控制中文输入法和撤销行为的裸 contenteditable。
6. 模型输出永远不是保存凭据。只有本地保存接口返回成功并完成读回校验后，系统才能显示“已保存”。
7. 需要确认的生成结果必须出现可见的预览、目标位置、写入方式、确认与丢弃按钮；不能只显示“等待确认”文字。
8. 用户在存在待确认事务时输入“确认”，客户端应接管并调用与按钮相同的确认操作，不能把该词作为普通消息继续发给模型。
9. 结构化页面必须通过其领域服务写主数据并同步投影：故事大纲走 StoryPlanningService，设定/风格/题材走 ProjectLibraryService；不得只写一个页面不读取的兼容文件。
10. 正文侧栏不得读取或覆盖当前完整 AI 助手会话。它使用一条独立的新会话，但该会话仍进入项目会话列表，用户可以在完整 AI 助手中继续同一对话。
11. 除侧栏首次建立的独立会话外，后续“新对话”只由用户显式创建；同一次编辑会话内切换章节不自动制造多个空会话，项目切换后必须解绑旧项目会话。
12. 侧栏快捷操作继续只把预设要求填入输入框，用户可以修改后发送；不因新增“扩写当前段落”改成自动请求或自动写入。
13. “导入小说”和“拆解小说”是两个连续但不同的动作：导入成功只进入待拆队列；同一本书拆解成功、报告完整并通过读回校验后，才进入正式拆书库。
14. 拆书库中的一本书只有一个稳定 bookId。重试、继续拆解和重新拆解都更新该书的同一 manifest 与版本，不得复制出同名新目录。
15. 正式拆书库只向融梗和蒸馏暴露 `ready` 书籍。单书可蒸馏；多书融梗继续保持当前至少 3 本的产品规则，但每一本都必须完成拆解。
16. 每本书的主阅读产物统一为 `拆书报告.md`；manifest 负责状态、版本、来源哈希和产物路径，Markdown 负责用户阅读、搜索、编辑与导出，二者职责不混用。
17. 蒸馏必须以已归档原文为主证据。创建蒸馏档案时覆盖全书不同阶段，启用蒸馏参与后续创作时同时注入档案与有来源标记的原文片段；不得静默改用拆书摘要冒充原文。
18. 融梗方案和蒸馏档案生成成功只代表“候选产物已生成”，不代表已进入正式大纲或风格库。必须显示目标预览并由用户确认应用，系统读回主数据后才能显示“已应用”。

## 3. 现状审计与根因

### 3.1 故事大纲红圈区域

相关代码：

- apps/workbench/src/features/product/outline/OutlineProductPage.tsx
- apps/workbench/src/styles.css
- apps/workbench/src/styles/components.css

现有 .beat-row 定义了四列：

~~~css
grid-template-columns: 16px 30px minmax(0, 1fr) 30px;
~~~

但组件实际只有“上下移动按钮组”和“情节点选择按钮”两个直接子元素。第二个子元素因此被塞进 30px 列，内部标题、状态、摘要和关联章节只能逐字换行，形成截图中的大面积空白与竖排文字。

此外：

- 中间情节点只能选择，不能直接修改。
- 右侧详情栏是唯一编辑入口，主内容区与编辑区割裂。
- 页面文案写着“拖动情节点”，实际只有上下箭头。
- 页面只有新增、保存和排序逻辑，没有删除情节点的处理函数、按钮或冲突处理。
- 多处内联样式使响应式调整和状态样式难以统一。
- 保存过程没有明确的保存中、失败、脏数据提示。

### 3.2 新建项目后的路由

相关代码：

- apps/workbench/src/features/product/home/HomeProductPage.tsx
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- apps/workbench/src/App.tsx

pickAndCreateProject() / performCreateProject() 已能完成项目创建与工作区刷新，但 submitNewProject() 在成功后只执行 closeCreation()。如果旧项目存在未保存内容，创建会先进入全局二次确认；确认后的创建流程也没有“创建来源”和“创建完成目标路由”的统一回执。

### 3.3 模型选择

相关代码：

- apps/workbench/src/features/settings/SettingsFeaturePage.tsx
- apps/workbench/src/features/product/assistant/AssistantModelControls.tsx
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- packages/conversation-service/src/service.ts

当前存在以下文本模型入口/语义：

1. 手动设置中的主模型。
2. 手动设置中的副模型。
3. 网站配置中的语言模型。
4. AI 助手中的会话级 model_override。

助手里的选择目前只调用 updateConversationModelPreferences()，不会同步活动配置的全局默认模型，因此不能满足“最近选择兼作全局默认”。

### 3.4 思考模式与流式输出

相关代码：

- packages/model-client/src/openai-compatible.ts
- packages/agent-runtime/src/chat-runner.ts
- packages/shared/src/schemas/agent.ts
- apps/desktop-shell/src/main/runtime/conversation-routes.ts
- packages/api-client/src/client.ts
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- apps/workbench/src/features/product/assistant/AssistantProductPage.tsx

已存在的链路：

~~~text
模型 SSE
  → OpenAICompatibleClient.streamCompletion()
  → ChatRunner.streamMessage() 生成 delta
  → 桌面本地服务输出 NDJSON
  → ApiClient 按行解析
  → Controller 累加 streamedText
  → AssistantProductPage 增量渲染
~~~

正文流已经打通。缺口在于：

- 模型客户端只提取 delta.content、delta.text 等正文字段，没有提取常见的 reasoning_content / reasoning / thinking 字段。
- AgentStreamEvent 的 delta 只有 text，没有“正文/思考”通道标识。
- 会话消息没有独立的思考内容字段。
- 前端只有正文气泡和“正在生成”，没有思考面板。
- 当前 reasoning_effort 默认是 medium，没有清晰的会话级“思考关闭/开启”状态。
- 开启去 AI 味时，用户先看到原始正文流，结束后可能被最终润色文本整体替换；需要明确显示“生成 → 润色 → 完成”的阶段，避免像流式内容回跳。

### 3.5 项目上下文参考链路

相关代码：

- packages/agent-runtime/src/chat-runner.ts
- packages/project-session/src/service.ts
- packages/document-service/src/story-planning-service.ts
- packages/document-service/src/project-library-service.ts

当前 AI 会话会自动拼装：

- 项目状态摘要；
- 大纲、细纲、章纲；
- 人物/体系/地图/道具设定；
- 风格与题材规则；
- 最近正文；
- 固定上下文与附件；
- 当前文档；
- 自动文件引用；
- 可用时的向量召回和已确认项目记忆。

因此“读取项目资料”的主链路仍然存在。但发现一个必须修复的断点：

- 新故事大纲页面通过 StoryPlanningService 写入 01_大纲/故事大纲.md。
- buildProjectContinuityContext() 的稳定上下文仍读取 01_大纲/大纲.txt。

这意味着用户在当前大纲 UI 中保存的新内容，不保证进入每次 AI 会话的稳定大纲上下文。设定资料链路相对完整：资料库会投影到 00_设定集/设定集/*.txt，连续性读取器包含这些兼容路径。

另外，助手右栏现有文案“AI 只会读取这里列出的内容”与实际实现不符，因为系统还会自动读取稳定项目上下文和检索结果。该文案会让用户错误理解隐私与参考范围。

### 3.6 AI 生成预览、确认与真实保存断链（P0）

相关代码：

- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- apps/workbench/src/features/product/assistant/AssistantProductPage.tsx
- apps/workbench/src/lib/workflow.ts
- packages/agent-runtime/src/skill-runner.ts
- packages/agent-runtime/src/runtime.ts
- packages/shared/src/schemas/agent.ts
- packages/api-client/src/client.ts
- apps/workbench/src/views/AgentTraceView.tsx

当前至少并存三种待确认结果：

1. 生成缓存 pending_save：包含 cache_id、目标路径和生成内容。
2. 资料库草稿 library_draft：用于设定、风格和题材，确认后才合并到结构化主数据。
3. Agent confirmation：用于需要批准的文件操作，带 run_id、confirmation_id、版本和作用域指纹。

新 AI 助手只把第一种转换成 pendingGeneratedSave。第二、第三种没有进入同一 UI 状态：

- handleAgentRunPayload() 遇到顶层 requires_confirmation 时，只显示“下一步把确认执行接进新工作台”的占位文案，没有加载确认详情或渲染按钮。
- AssistantProductPage 只有 pendingGeneratedSave 存在时才显示写入按钮，而且当前卡片只显示目标和字数，没有展示实际生成正文或差异。
- 如果没有 pendingGeneratedSave，用户输入“确认”会作为普通消息继续发给模型，模型回复并不会触发 saveGeneratedResult()、资料草稿 commit 或 Agent confirmation approve。
- Agent confirmation 的查询、批准和拒绝 API 已经存在，但目前主要由 AgentTraceView 使用，产品 AI 助手没有接入。

更严重的是，多个 Runtime 包装函数把 requires_confirmation 硬编码为 false，包括 skillResponseToAgentResponse()、runLocalSkillIntent()、streamPromptSkill() 的 final payload、durableSkillAgentResponse() 和智能技能编排的最终响应。它们会覆盖 skill_result.data.requires_confirmation 的真实语义。于是底层明明创建了待确认草稿，助手顶层状态却显示“不需要确认”。

截图中模型声称“文件已保存：01_大纲/章纲.md”，也不能视为系统保存结果：项目的规范章纲路径是 01_大纲/章纲.txt，且截图没有系统写入回执、保存路径读回或版本号。这进一步说明当前消息气泡混淆了“模型叙述”和“系统事实”。

### 3.7 风格、题材、设定与全软件同类问题审计

风格、题材、设定并不是普通文本文件的单一覆盖。ProjectLibraryService 以以下结构化文件为主数据，再生成 TXT 兼容投影：

- 00_设定集/.agent/libraries/lore.v1.jsonl
- 00_设定集/.agent/libraries/style.v1.jsonl
- 00_设定集/.agent/libraries/genre.v1.jsonl

style_extract、genre_generate、lore_extract 会先创建 00_设定集/.agent/library-drafts/*.jsonl，并明确标记 requires_confirmation: true。旧 ProjectLibraryPages 已有“待确认 AI 草稿”组件，但当前 ProductWorkspace 实际加载的是新版 SourcesProductPage 和 StyleProductPage；这两个产品页没有挂载草稿审核组件。因此草稿即使成功落盘，用户在当前界面也看不到、无法确认，结构化主数据和兼容投影自然不会更新。

故事大纲存在另一种同类错位：当前 OutlineProductPage 读取 StoryPlanningService 的 00_设定集/.agent/story-planning.jsonl，并投影到 01_大纲/故事大纲.md；AI 的 outline_generate 默认目标仍是 01_大纲/大纲.txt。后者写入成功也不会让当前故事大纲页面更新。

全软件主要 AI 写入入口审计如下：

| 写入入口 | 当前持久化方式 | 审计结论 | 本轮处理 |
| --- | --- | --- | --- |
| AI 助手普通生成并写文件 | generated cache + save plan | pending_save 分支可保存，但新助手缺完整内容/差异预览；其他确认类型会丢失 | 纳入统一待写入事务 |
| 大纲、细纲、章纲、正文、续写、润色、去 AI 味技能 | generated cache 或显式自动提交 | 单技能 pending_save 基础链路存在；智能多技能编排可能在最终包装时丢失确认状态 | 统一响应契约并逐技能回归 |
| 结构化故事大纲 | StoryPlanningService 主数据 + Markdown 投影 | AI 默认仍写旧大纲 TXT，页面与上下文可能不更新 | 将“故事大纲”语义路由到领域服务 |
| 设定、风格、题材 | ProjectLibraryService 草稿 + JSONL 主数据 + TXT 投影 | 草稿生成成功但确认状态被覆盖，新版目标页也不可见 | 助手和目标页同时接入资料草稿审核 |
| 删除、归档等文件操作 | durable Agent confirmation | API 与追踪页存在，产品助手未接入 | 助手展示操作差异并调用批准/拒绝 API |
| 批量章节生成 | 后台任务 + approved draft | 已有“先到批次草稿、不覆盖正式正文”语义 | 保留现状，加入统一系统回执与回归测试 |
| 素材迁移 | 专用预览 + 明确提交按钮 | 已具备预览和确认界面 | 保留现状，复用统一成功校验规则 |
| 拆书、融梗与蒸馏 | 导入即写正式书库、拆解又新建目录，完成态靠任意文件猜测，报告仍是分散 TXT | 统一 bookId、显式状态机和 Markdown 报告；只有 ready 书籍可融梗/蒸馏并以系统回执确认完成 |
| 封面、审阅报告等独立产物 | 专用 API 直接保存 | 不属于对话式确认，但成功提示同样应基于返回路径和文件存在 | 加读回/存在性验收，不改成交互确认 |
| 技能编辑与导入 | dry-run diff + 显式保存 | 已有预览再提交语义 | 保留现状，纳入防回归测试 |

因此不建议分别给“故事大纲”“风格与题材”增加临时按钮，而应先统一待确认结果的契约、展示和系统回执，再让每个领域提供自己的 commit 适配器。

### 3.8 正文侧栏并非独立对话

相关代码：

- apps/workbench/src/features/product/editor/EditorProductPage.tsx
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- apps/workbench/src/styles.css
- packages/api-client/src/client.ts
- packages/conversation-service/src/service.ts

截图中的右栏目前不是真正的小型聊天线程：

- latestAssistantMessage 直接从全局 controller.conversationDetail 取最后一条 assistant 消息，所以会把用户此前在完整 AI 助手中的长回复塞进 318px 侧栏。
- 侧栏不显示本轮用户消息和连续上下文，只显示这一条旧 AI 回复与“进入完整对话”，因此既不像摘要，也无法在原地连贯聊天。
- 输入框复用全局 controller.messageInput，侧栏输入会覆盖完整 AI 助手尚未发送的草稿。
- send() 调用 controller.sendMessage()；底层 sendConversationPrompt() 会 setActiveTab("conversations")，用户从正文侧栏发送后会被切离正文编辑页，不能完成“直接在侧边栏对话”。
- ensureConversationId() 和 createConversation() 都围绕全局 conversationDetail 工作，createConversation() 还会主动进入完整会话页，当前没有“创建但不激活全局页面”的接口。
- 上下文条同样读取全局会话的 pinned_context，可能展示和移除另一条完整对话中的固定上下文。
- 快捷操作当前只通过 handleShortcut() 填充全局 messageInput，不会立即发送。三个现有操作必须保持这一语义。

编辑器已经能通过 textarea.selectionStart / selectionEnd 得到光标与选区，但没有“当前段落”解析器。新增扩写前必须把选区、段落范围、文档路径与基线版本绑定在一起，避免 AI 返回后光标已移动或文档已改变却仍写错位置。

### 3.9 拆书流程、产物与蒸馏原文链路错位

相关代码：

- apps/workbench/src/features/product/disassembly/DisassemblyProductPage.tsx
- apps/workbench/src/features/disassembly/DisassemblyFeaturePage.tsx
- apps/workbench/src/features/project/ProjectSidebar.tsx
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- packages/agent-runtime/src/workflows/disassemble-book.ts
- packages/agent-runtime/src/workflows/disassemble-library.ts
- packages/agent-runtime/src/workflows/book-fusion.ts
- packages/agent-runtime/src/workflows/nuwa-style-distill.ts
- packages/project-session/src/service.ts
- apps/desktop-shell/src/main/runtime/job-routes.ts

当前实现与用户期望的“导入 → 拆解 → 入拆书库 → 选择融梗或蒸馏”存在六个断点：

1. `archive_source` 在导入时就通过 `createDisassembleBook()` 写入 `00_设定集/拆书库/<bookId>/原文.txt` 和 manifest，界面立即把它当作书库条目，只用“等待拆解”小字区分，没有独立待拆队列。
2. 对该条目执行一键拆解时，`runFullDisassemble()` 读取已有原文后仍再次调用 `createDisassembleBook()`，从而生成第二个 bookId/目录。导入条目继续显示“等待拆解”，拆解产物落到另一条记录，正是截图中逻辑对不上的核心原因。
3. manifest 没有 `status`、错误、进度、分析版本和完整性字段。前端与融梗后端都把“lore/reverse_outline/detail_outline 任意一个路径存在”当作已完成，部分失败也会错误进入可融梗列表。
4. 一键拆解只生成 `拆书设定提取.txt` 和 `反向细纲.txt`，还会覆盖全局 legacy 文件；没有一份对应用户阅读习惯的统一 Markdown 报告，也没有保证 1–489 章这类长篇被全量覆盖。
5. 上传和拆解读取都受 20 万字符上限约束；Nuwa 蒸馏进一步只取来源前 6 万字符。当前策略会静默忽略长篇后半段，无法支撑跨全书的大事件、成长弧和文风结论。
6. 界面虽然向蒸馏传入 `source_book_id`，但 Nuwa 来源解析器并不使用该 id 校验书籍，只按 `text/source_path/附件/旧拆书产物` 逐级兜底。创建档案后，`buildProjectContinuityContext()` 只注入 `profile_text` 和来源摘要，后续创作并没有注入原文证据。因此“蒸馏时使用原文”和“使用蒸馏文风时带原文参考”都未形成强约束。

#### 融梗与蒸馏实际落库检查

当前两项功能都没有落入用户理解的正式目标库：

| 功能 | 当前实际保存位置 | 是否进入正式目标 | 后续创作读取情况 |
| --- | --- | --- | --- |
| 融梗 | `00_设定集/融梗方案/<fusionId>/融梗候选.txt`，另存提示词、来源书籍和 manifest | **不进入大纲**。即使 `output_mode=outline`，它也只改变提示词/元数据，保存路径仍是融梗候选目录 | 大纲上下文不会自动读取该候选；必须人工另行应用 |
| 蒸馏 | `00_设定集/.agent/style_distillation/current.json` | **不进入普通风格库**，不会写 `写作风格.txt`、`风格示例.txt`、`参考素材.txt` 或 style 结构化主数据 | 启用后会作为 `continuity.style` 参与创作，但当前实现是替代普通风格库，而不是与风格库合并 |
| 拆书页“提取写作方法/生成迁移预览” | `style_extract` 会创建 `00_设定集/.agent/library-drafts/*.jsonl` 风格草稿 | 草稿未经确认不会进入正式风格库；当前产品页又没有草稿确认入口 | 顶层 `requires_confirmation` 还可能被包装器覆盖，用户容易误以为已应用 |

因此“模型生成完成”和“正式资料已更新”目前是两回事：融梗缺少应用到大纲的 commit 入口；蒸馏虽能影响生成，却在风格库页面不可见、不可按普通风格资料管理；规则迁移则停在隐藏草稿。三者都需要接入 4.6 的统一预览、确认、提交与系统回执。

另有两个扩大混乱的兼容面：旧 `DisassemblyFeaturePage` 已没有产品路由引用，却保留另一套当前文档兜底和操作文案；项目树侧栏也能直接触发蒸馏。修改不能只修截图页面，必须让所有入口调用同一领域服务和状态判定。

指定 Notion 精确链接当前对本环境返回无权限，但同一空间中检索并读取到了标题与章节范围匹配的 [《怪物收容监狱/749陆鼎》剧情拆解与大事件（1–489章）](https://app.notion.com/p/cbce975c28288264acf9819521484e22)。其信息结构可归纳为：核心设定速览、逐章速览、黄金三章与前十章、按章节范围组织的大事件及起承转合、可复用创作模板、主角成长弧。方案只复用结构，不复制示例作品内容。

## 4. 详细修改设计

### 4.1 重做故事大纲中间区

#### 目标结构

大屏继续保留三段式工作区，但重新划分职责：

~~~text
故事结构导航（分卷/线索）
  │
  ├── 情节点工作区（主要内容，可就地编辑）
  │     ├── 本卷目标 / 核心冲突 / 情绪走向
  │     └── 情节点列表
  │
  └── 详情栏（元数据、关联对象、一致性结果、删除等高级操作）
~~~

情节点行改为两层结构：

- 左侧 44px：拖动把手、序号和键盘可用的上移/下移操作。
- 右侧 minmax(0, 1fr)：标题、状态、摘要、关联章节。
- 浏览态保持紧凑；点击行或编辑按钮后，当前行展开为编辑态。

#### 就地编辑行为

编辑态提供：

- 标题输入框；
- 状态选择；
- 剧情目标/摘要多行输入；
- 关联章节标签输入；
- “保存”“取消”；
- Ctrl + Enter 保存、Esc 取消；
- 未保存提示与保存失败提示。

只允许一个情节点同时处于编辑态。切换情节点时，如果当前内容未保存，先在当前行内显示“保存 / 放弃”，不优先弹模态框。

右侧详情栏不再重复相同的标题与摘要编辑，以免产生两个编辑源。它用于：

- 父级/分卷归属；
- 关联人物、地点、伏笔等高级关系；
- 一致性检查结果；
- 删除情节点；
- 版本与最近保存时间。

#### CSS 与响应式

- 删除当前四列 .beat-row 结构，使用与 DOM 一致的两列网格。
- 中间列必须有 min-width: 0，长文本按正常中文行宽换行。
- 统一使用 4pt 间距体系：4、8、12、16、24、32。
- 控件最小点击目标 44×44px；视觉图标可以较小。
- 将大纲专用样式从巨型 styles.css 迁入独立的大纲样式文件或 product-pages.css，移除对应内联样式。
- 容器宽度充足时显示三栏；中等宽度将右栏收为可展开详情面板；窄窗口只保留导航与主编辑区，禁止文档级横向滚动。

#### 数据与并发

- 继续使用 base_revision 乐观并发控制。
- 保存中禁用同一节点的重复提交。
- 遇到 revision conflict 或 projection drift 时保留本地草稿，提示刷新/重试，不静默覆盖。
- 排序后重算 order，拖动与键盘上移/下移走同一保存函数。

#### 删除情节点

删除入口放在右侧详情栏的“危险操作”区，并在选中行悬停/键盘聚焦时提供同义快捷入口。首次点击不立刻删除，而是在原位置展开影响说明和“确认删除 / 取消”。

规则：

- 叶子节点：显示标题、关联章节数量，确认后从 outline 中移除。
- 存在子节点：提供“删除本节点并将子节点提升到上级”和“连同全部子节点删除”两个明确选项；默认不预选破坏性更高的整棵删除。
- 删除前统计受影响的子节点、章节关联和人物/地点等实体关联，并在确认区显示。
- 提交时使用当前 base_revision；发生并发冲突时不在客户端强行重放删除，保留确认状态并要求刷新影响清单。
- 删除成功后重新计算 order，选中相邻的下一个节点；没有下一个节点时选中上一个或父节点。
- 保存进入 DocumentService 时间线，允许从版本历史回滚；系统回执显示删除数量和新 revision。
- 取消删除不修改本地草稿、选择和排序。

删除不是“保存详情”的隐式副作用，应使用单独的领域命令，例如 deleteStoryOutlineNode({ nodeId, strategy, baseRevision })，便于测试影响范围和幂等性。

### 4.2 新项目创建后进入 AI 助手

实现一个只针对“创建”的完成回执，不用监听所有项目路径变化后盲跳转。

流程：

1. 用户提交小说名称并选择父目录。
2. 创建项目并完成 finalizeProjectSwitch()。
3. 如当前项目原有未保存内容，用户完成二次确认后再进入第 4 步。
4. 路由跳转到 /assistant。
5. 新项目没有会话时创建一个“新对话”，让输入框直接可用。

边界：

- 取消目录选择：留在首页，不跳转。
- 创建失败：留在首页并显示可操作错误。
- 普通“打开项目”和“打开最近项目”：保持原行为，不自动跳转 AI 助手。
- 二次确认被取消：保持旧项目与当前路由。
- 使用 pushState，允许用户点击返回回到项目首页。

建议让控制器返回结构化结果，例如 { status, project, source: "create" }，由页面执行导航；不要用全局项目路径变化推断“刚刚创建”。

### 4.3 将文本模型选择统一到 AI 助手

#### 设置页

手动配置：

- “模型”页签改名为“连接参数”。
- “主模型”改为“主线路”，保留 API Key、Base URL、temperature、top_p。
- “副模型”改为“备用线路”，保留副 API Key、副 Base URL、temperature、top_p。
- 移除主模型选择/刷新控件和副模型文本框。

网站配置：

- 移除“语言模型”下拉框。
- 保留网站登录、账号、余额、充值、temperature、top_p 等连接/生成参数。
- 保留封面生图模型与向量模型入口。

兼容策略：

- 不删除旧配置文件中的 model / secondary_model 字段，避免旧用户配置迁移时丢失。
- UI 不再直接编辑这些字段。
- 尚未在助手中选择新模型时，旧的 model 继续作为初始全局默认。

#### AI 助手模型弹层

模型弹层成为唯一文本模型选择入口：

- 显示当前全局默认标记。
- 显示来源、能力和是否支持思考。
- 支持搜索与刷新模型列表。
- 用户显式选择模型后，按顺序执行：
  1. 保存当前会话的 model_override；
  2. 保存活动配置（手动或网站）的全局默认模型；
  3. 更新其他 AI 功能随后使用的模型配置；
  4. 成功后显示“已设为当前会话模型和全局默认”。
- 任一步失败时显示具体失败阶段；不得出现 UI 已选中但全局配置未保存的假成功。

“跟随全局默认”可以保留，用于让旧会话解除覆盖；它不是另一个模型入口。只有用户显式点击某个模型时才更新全局默认，单纯打开旧会话不会改变全局默认。

### 4.4 思考模式、思考内容与正文双通道流

#### 会话偏好

在 ConversationModelPreferences 增加：

- reasoning_enabled: boolean，默认 false；
- 保留 reasoning_effort: low | medium | high。

模型弹层改成：

- “思考模式”开关；
- 开启后才允许选择低/中/高；
- 不支持思考的模型禁用开关并解释原因；
- 关闭时请求中不发送 reasoning_effort 和 thinking；
- 只支持一个等级的模型自动锁定到该等级。

#### 模型客户端

不要直接把所有思考文字混进现有字符串流。新增兼容的详细流接口，例如：

~~~ts
type ModelStreamChunk =
  | { channel: "reasoning"; text: string }
  | { channel: "answer"; text: string };
~~~

解析器需要覆盖当前 OpenAI-compatible 中转常见返回：

- choices[].delta.reasoning_content
- choices[].delta.reasoning
- choices[].message.reasoning_content
- 可识别的 thinking 文本块
- 原有 delta.content / delta.text / message.content

保留旧 streamCompletion(): AsyncGenerator<string> 作为正文兼容包装，避免一次性破坏技能、批量生成等现有调用；AI 会话改用详细流接口。

非流式回退也要能分别提取思考与正文。若服务端忽略 stream=true，最终仍能返回完整正文，并在有思考字段时展示思考内容。

#### Runtime、传输与存储

- AgentStreamEvent 的 delta 增加 channel: "reasoning" | "answer"；旧事件缺省为 answer。
- ChatRunner 分别累加 reasoningParts 与 replyParts。
- 最终会话消息增加向后兼容的 reasoning_content 字段，正文仍存放在 content。
- 停止生成时分别保存已有思考片段和正文片段，并标记 stopped。
- 桌面服务输出响应头后立即 flush；每个 NDJSON 事件独占一行并立即写出。
- API Client 分发不同 channel，Controller 使用两个缓冲区，不能再只有 streamedText。

#### AI 助手 UI

AI 消息结构调整为：

1. 可折叠“思考过程”区域；
2. 最终正文；
3. 写入预览/确认操作。

交互规则：

- 思考模式开启且收到首个思考增量时自动展开，标题显示“正在思考”。
- 首个正文增量到达后标题改为“思考完成”，思考区默认折叠，避免压过正文。
- 生成结束后保留折叠入口，可再次查看。
- 开启思考但模型未返回思考字段时，在结束后显示一次轻量说明，不制造空白大面板。
- 关闭思考模式时完全不渲染该区域。
- 正文继续逐步输出并保留停止按钮。
- 增量渲染按 requestAnimationFrame 或 30–50ms 节流，避免每个极小 token 都触发完整 Markdown 重解析。
- aria-live 只播报阶段变化和完成状态，不逐 token 朗读。

如果启用了去 AI 味：

- 原始正文仍可流式显示；
- 流结束后显示“正在润色”阶段；
- 最终替换时给出明确状态，不让用户误以为流式内容丢失。

### 4.5 修复并可视化项目上下文链路

#### 统一稳定上下文来源

调整 buildProjectContinuityContext()：

- 总纲优先读取 01_大纲/故事大纲.md；
- 该文件不存在或为空时回退 01_大纲/大纲.txt；
- 细纲与章纲继续读取现有 细纲.txt / 章纲.txt；
- 设定继续兼容 设定库 与 设定集 两种路径；
- 风格、题材、最近正文继续保留；
- 对重复或已过期的旧投影不同时注入，避免模型看到两个互相冲突的总纲。

同时让故事大纲保存后的索引刷新、项目记忆失效和 AI 稳定上下文刷新走同一条 artifact 更新链路。

#### 修正文案与上下文可见性

AI 助手右栏拆成两组：

- “自动项目上下文”：大纲、设定、风格/题材、最近正文、项目记忆；显示是否读取成功和最近更新时间。
- “本次额外上下文”：用户固定的文档、附件、当前文档。

将“AI 只会读取这里列出的内容”改为：

> AI 会自动读取项目大纲、设定与近期正文；你还可以为本次会话补充文档和附件。

首次实现不在右栏铺开完整文件内容，只展示来源、状态和数量；用户点击后再查看摘要，避免三栏同时争夺注意力。

#### 可验证性

在上下文拼装层输出只含来源与字符数的 context_manifest，例如：

~~~json
[
  { "id": "outline", "path": "01_大纲/故事大纲.md", "included": true, "chars": 2180 },
  { "id": "lore", "path": "00_设定集/设定集/人物设定.txt", "included": true, "chars": 1420 }
]
~~~

该清单供 UI 状态和测试使用，不记录 API Key，也不复制完整正文。

### 4.6 统一“生成 → 预览 → 确认 → 提交 → 校验”写入事务

#### 用判别联合替代单一布尔值

顶层 requires_confirmation 可以保留为兼容字段，但不能再作为唯一契约。新增结构化 pending_action，例如：

~~~ts
type PendingWriteTransaction =
  | { kind: "generated_cache"; transactionId: string; cacheId: string; targets: WriteTarget[]; preview: PreviewPayload }
  | { kind: "library_draft"; transactionId: string; draftId: string; domain: "lore" | "style" | "genre"; preview: PreviewPayload }
  | { kind: "agent_confirmation"; transactionId: string; runId: string; confirmationId: string; operations: OperationPreview[] }
  | { kind: "story_planning_patch"; transactionId: string; baseRevision: number; patch: StoryPlanningPatch; preview: PreviewPayload };
~~~

每一种事务统一具有：

- transaction_id、conversation_id、来源技能和创建时间；
- 明确的领域与规范目标，不只是一段模型生成的路径文字；
- 完整预览或可分页加载的 diff；
- 允许的写入方式、风险说明和冲突基线；
- awaiting_confirmation / committing / committed / failed / conflict / discarded 状态；
- 提交后的 saved_paths、revision、content_hash 和 timeline_entry_id。

所有 AgentRunResponse 包装器都从实际 skill_result、run confirmation 或领域草稿推导 pending_action，禁止再次硬编码 false。智能多技能编排只要任一步仍有待确认事务，最终响应就必须保留这些事务，不能被最后一步结果覆盖。

#### AI 助手中的事务卡

每条待确认事务紧跟产生它的助手消息显示，不依赖“最后一条助手消息”猜测归属。事务卡至少包含：

1. 系统状态：“尚未写入项目”。
2. 规范目标：页面/资料域名称与真实路径。
3. 变更摘要：新增、修改、删除数量；覆盖时显示旧/新差异。
4. 可展开的完整生成内容或分段预览，不能只有字数。
5. 允许的操作：确认写入、丢弃；仅通用文本目标提供追加/覆盖/另存草稿。
6. 冲突、权限、投影漂移等错误在卡内显示，失败后保留事务以便重试或复制内容。

资料草稿确认后进入对应的设定/风格/题材页；故事大纲 patch 确认后进入大纲页并选中新节点；通用文本写入后可以打开目标文档。

#### 文字“确认”的兼容行为

- 当前会话恰好有一个 awaiting_confirmation 事务，且输入规范化后仅为“确认/确认保存/确认写入”时，客户端弹出同一事务卡并调用与按钮完全相同的 commit 函数。
- 有多个待确认事务时不猜测，列出目标要求用户点选。
- 输入包含其他创作要求时仍作为新消息，但先提示存在未处理事务。
- 确认、丢弃和提交中的事务均不再发送给语言模型。

#### 领域目标解析

由系统根据意图、当前页面、技能和项目 manifest 解析目标，模型只能提出候选，不能自证路径正确：

- “故事大纲/主线大纲/情节点” → StoryPlanningService 结构化 patch，同时更新 story-planning.jsonl 与 故事大纲.md。
- “细纲/章纲” → 现有 细纲.txt / 章纲.txt，除非未来建立独立结构化服务。
- “设定/人物/体系/地图/道具” → ProjectLibraryService 的 lore 草稿。
- “风格/范文/参考素材” → ProjectLibraryService 的 style 草稿。
- “题材/战斗模板/违禁词” → ProjectLibraryService 的 genre 草稿。
- “当前正文/指定文档” → generated cache，以项目内规范相对路径为目标。

截图中的 01_大纲/章纲.md 不在当前规范 manifest 中，应在预览阶段标为“模型建议路径不可用”，自动解析为 01_大纲/章纲.txt 或要求用户选择，绝不能显示为已保存路径。

#### 真实保存与系统回执

提交动作必须满足以下条件后才进入 committed：

1. 调用对应的 cache commit、library draft commit、Agent approve 或 StoryPlanning save API。
2. API 返回非空的实际保存路径或新的领域 revision。
3. 服务端按预期 hash/revision 读回主数据，并确认投影同步成功。
4. 刷新项目文件树、打开文档状态、目标领域页面、向量索引和稳定 AI 上下文。
5. 由系统消息生成不可由模型伪造的保存回执，例如“系统已写入 · 故事大纲 · revision 8 · 2 个文件”。

模型气泡中的“已保存”“已写入”按普通文本渲染，不显示成功图标；只有带系统 receipt 元数据的消息使用成功样式。提交失败时不得清除预览，不得让后续模型回复覆盖失败状态。

### 4.7 新版资料页面补回待确认草稿

将旧 ProjectLibraryPages 中已验证的 LibraryDraftReview 能力抽成共享产品组件，分别接入：

- SourcesProductPage：只显示 lore 草稿。
- StyleProductPage：按当前页签突出 style 或 genre 草稿，同时允许查看另一域待确认数量。
- AssistantProductPage：显示由本次会话产生的草稿事务，确认后通知目标页刷新。

草稿卡不能只列前三个名称。应支持展开查看每条记录的名称、类型、摘要、目标域和“新增/与现有同名”判断；确认前允许取消勾选不需要的记录。提交使用当前资料库 revision 合并，遇到 projection drift 或同名冲突时留在预览态处理，不能静默追加重复记录。

目标页顶部显示全局待确认数量；即使用户离开产生草稿的会话或重启应用，也能从资料页恢复处理。

### 4.8 正文侧栏改为独立迷你 AI 对话

#### 会话隔离

新增 EditorAssistantThread（可实现为独立 hook + 展示组件），不再直接绑定全局 conversationDetail、messageInput 和 conversationMessage。它维护：

- editorConversationId / editorConversationDetail；
- editorMessageInput；
- editorSending、editorStreamedReasoning、editorStreamedAnswer；
- 当前侧栏会话的待确认写入事务；
- 当前项目 ID 和最近关联的正文路径。

会话规则：

1. 进入正文编辑页时不读取当前完整 AI 助手会话。
2. 侧栏第一次可用时创建一条新的正文侧栏会话，标题为“正文助手 · 章节名”；同一次应用编辑会话内重复收起、展开或切换章节继续使用它。
3. 点击标题栏的“新对话”才创建另一条侧栏会话；有未处理写入事务时先在侧栏内提示处理，不直接丢弃。
4. 切换项目时清除旧 editorConversationId，并为新项目创建或等待创建新的侧栏会话，禁止跨项目复用。
5. 新会话写入正常的项目会话列表，并标记 source_surface: "editor_sidebar"。点击“在 AI 助手中打开”时加载同一 conversationId，不复制消息、不另建对话。
6. 侧栏会话默认跟随全局默认模型和思考开关，不在正文侧栏增加第二个模型选择入口。

为避免仅仅浏览正文就产生大量空会话，可采用“显示临时空态，第一次输入/快捷操作时落盘创建”的惰性创建；UI 上仍把它表现为一条全新的空对话。Controller 需要提供不切换 activeTab 的 createEditorConversation()，以及接受显式 conversationId 的 streamConversationPrompt()，不能临时替换全局 conversationDetail 后再复用旧函数。

#### 迷你对话框结构

侧栏保持约 300–320px 宽，但内容改为真正的紧凑线程：

~~~text
正文 AI                 ＋新对话  ↗  收起
当前章节 · 正文.txt       本段 46 字
────────────────────────────────
你：把这一段的压迫感再加强……
AI：正在思考…
AI：可以从环境反应和动作迟滞……
  [待写入预览（如有）]

快捷操作
[续写当前段落] [扩写当前段落]
[检查人物口吻] [增强悬念]

输入修改或分析要求……          发送/停止
~~~

- 删除当前占满侧栏的“全局最后一条 AI 回复”卡和默认“进入完整对话”按钮。
- 消息区显示当前侧栏会话内的 user / assistant / system 消息，用户消息右对齐，AI 与系统状态左对齐；不使用多层嵌套卡片。
- 正文和思考沿用 4.4 的双通道流；思考区在窄侧栏中默认折叠，只显示“正在思考/思考完成”。
- 写入预览沿用 4.6 的事务卡紧凑变体，确认仍需真实 commit 与读回校验。
- 空态显示“针对当前章节提问，或使用下方快捷操作”，不回填其他会话内容。
- 消息结束后自动滚动到底部；用户主动上滚后暂停自动追随，并显示“回到最新”。
- 输入框使用本地草稿；Enter 发送、Shift + Enter 换行，生成中发送按钮变为停止。
- 状态和错误留在侧栏内，不通过 setActiveTab("conversations") 把用户带离正文。

标题栏保留“在 AI 助手中打开”图标作为渐进入口，但它不是默认流程。用户可以在完整页面查看较长历史、模型思考、附件和复杂写入预览。

#### 上下文策略

- 当前正文路径通过请求 current_path 自动传入，不需要每轮手动固定“当前章节”。
- 标题下只显示只读上下文摘要，例如“当前章节 · 已自动读取大纲/设定”；管理更多上下文时再进入完整 AI 助手。
- 切换章节后下一轮请求使用新 current_path，并在消息分隔处显示“上下文已切换到 第 12 章”；旧消息保留原 path 元数据，便于追溯。
- 如果当前正文有未保存修改，请求使用内存中的当前内容/选区作为 turn context，并明确标记 revision 或 content hash；不能只让服务端读取旧磁盘版本。

#### 保留快捷操作并增加“扩写当前段落”

保留现有三个按钮、顺序与“只填入输入框、不自动发送”的行为：

1. 续写当前段落。
2. 检查人物口吻。
3. 增强悬念。

新增“扩写当前段落”，建议排在“续写当前段落”之后。语义必须与续写区分：

- 续写：在当前段落之后继续情节。
- 扩写：不推进新的关键事件，在当前段落内部补足动作、感官、环境反应和情绪层次，保持人称、时态、文风与既有事实。

目标文本解析规则：

1. 有非空选区时以选区为目标。
2. 没有选区时取光标所在的非空段落；兼容 CRLF/LF，优先按空行分段，对“一段一行”的正文回退到当前行。
3. 光标位于空白段时向上寻找最近非空段，并在输入框上方显示选中的段落摘要；找不到则禁用按钮并提示“请把光标放到要扩写的段落”。
4. 记录 start、end、selectedText、documentPath、documentRevision/contentHash。发送时把目标内容放入结构化 selection/context 字段，不依赖模型从整个章节中猜测“当前段落”。

生成结果只出现在迷你对话中。用户进一步要求替换或写入时，进入统一写入事务，预览中显示原段与扩写后段落；文档基线已变化则标记冲突，不能按旧 start/end 直接覆盖。

#### 响应式与可访问性

- 大屏保留三栏：章节 208px、正文 minmax(0,1fr)、AI 318px。
- 中等宽度侧栏缩到 285–300px，快捷操作变为两列或自然换行，正文最小宽度优先于侧栏。
- 小于约 1080px 时默认收起侧栏，通过现有右下角按钮打开右侧抽屉；抽屉覆盖章节边缘而不压缩正文到不可写宽度，也不产生页面横向滚动。
- 所有图标按钮提供 title 与 aria-label；新对话、打开完整对话、停止生成和回到最新都可键盘操作。
- 消息状态使用 aria-live="polite" 只播报发送、思考、完成和错误，不逐 token 朗读。

### 4.9 重构拆书为“待拆队列 → 正式书库 → 融梗/蒸馏”

#### 工作台信息架构

截图页面继续保留左、中、右三栏，但各栏只承担一种职责：

~~~text
待拆队列 / 拆书库       当前书籍与拆书报告               对已完成书籍执行动作
─────────────────────────────────────────────────────────────────────
待拆解（1）             导入信息、进度、错误与重试          [蒸馏这本书]
拆书库（12）            或完整 拆书报告.md 预览             [加入融梗选择]
失败（1，可筛选）        打开 Markdown / 重新拆解            已选 3 本 → 生成融梗方案
~~~

- 导入完成后选中“待拆解”条目，主按钮为“开始拆解”，而不是含义不清的全局“一键拆解”。
- 拆解中显示阶段、章节覆盖率、已处理/总章节、停止和失败重试；刷新页面后从 checkpoint 恢复。
- 拆解完成后同一条目从“待拆解”进入“拆书库”，bookId 和目录不变，自动打开报告总览。
- 正式书库支持搜索、状态/题材筛选和多选。单选显示“蒸馏”，多选区显示“融梗”；未完成条目不出现可执行按钮。
- 右栏原“融梗与蒸馏”和“应用到当前项目”不再同时堆满。先选择动作，再渐进显示该动作的参数和结果；规则迁移仍保留为报告中的次级操作，继续走预览确认。

#### 单书状态机与稳定标识

为 `DisassembleBookManifest` 增加显式字段，禁止前端再根据路径数量猜状态：

| 状态 | 含义 | 允许操作 |
| --- | --- | --- |
| `imported` | 完整原文已归档并校验，尚未拆解 | 开始拆解、删除导入、查看原文 |
| `analyzing` | 正在按章节拆解，有 checkpoint 和进度 | 查看进度、停止、恢复 |
| `ready` | 报告与机器产物完整、读回通过 | 查看/编辑报告、蒸馏、加入融梗、重新拆解 |
| `failed` | 某阶段失败，保留原文与已完成 checkpoint | 查看错误、从失败阶段重试 |
| `stale` | 原文或模板版本变化，旧报告仍可读但需重建 | 查看差异、重新拆解；默认不参与新融梗/蒸馏 |

manifest 至少记录：

- `schema_version`、`status`、`analysis_version`、`template_version`；
- `source.path/hash/chars/chapter_count/import_complete`；
- `progress.stage/completed_chapters/total_chapters/last_error`；
- `coverage.first_chapter/last_chapter/analyzed_chapters/missing_chapters`；
- `paths.source/report/chapter_index/evidence_index`；
- `created_at/updated_at/analyzed_at`。

`archive_source` 只创建一次 bookId。`disassemble_book` 收到 `source_book_id` 后必须锁定并更新该 manifest，严禁再调用“创建新书”；没有显式 id 时也不能默认拿书库第一本，应要求用户选择或先创建导入记录。并发点击通过 bookId 级运行锁去重。

导入必须保存完整原文并记录字符数、哈希与章节索引。现有 20 万字符读取上限改为流式/分块读取上限，不允许静默截断；若文件超过产品支持上限，应在导入前明确提示实际限制和未导入范围，不能显示“导入成功”。

#### 长篇拆解流水线

1. 按可靠章节标题建立 `章节索引.jsonl`；没有章节标题时按稳定段落窗口切块，并保存起止偏移。
2. Map 阶段逐批生成章节速览、人物/设定事实、事件节点和文风证据；每批记录章节范围、原文 hash 和 checkpoint。
3. Reduce 阶段合并人物别名、事件边界、伏笔、成长弧与可复用方法，发现章节缺口时标为“未分析”，不得脑补。
4. Report 阶段生成临时 `拆书报告.md`，校验必需标题、章节覆盖率、来源范围和 Markdown 可解析性。
5. 报告与索引读回成功后原子更新 manifest 为 `ready`；任一步失败保持 `failed/analyzing`，不得提前进入正式书库。

现有 `拆书设定提取.txt`、`反向细纲.txt` 可在迁移期作为中间产物读取，但新流程不再把每本参考书自动覆盖到项目级 legacy 路径。只有用户明确执行“应用到当前项目”，经过预览确认后，才能写入项目大纲或资料库。

#### `拆书报告.md` 模板

Notion 的折叠块在本地 Markdown 中改为标准标题层级；折叠/展开由工作台预览器实现，文件本身保持 CommonMark/GitHub Markdown 可读。每本书默认生成：

~~~markdown
# 《书名》剧情拆解与大事件（第 1–N 章）

> 拆解范围、原文字数、生成时间、模板版本与覆盖率说明。

## 核心设定速览
### 主角与初始身份
### 核心前提 / 金手指
### 世界与规则
### 核心爽点循环

## 逐章速览
### 第一阶段 / 大事件名称（第 X–Y 章）
- 第 X 章：一句话剧情推进

## 黄金三章与前十章
### 黄金三章拆解
### 前十章阶段推进
### 开篇钩子、兑现与章末悬念

## 大事件拆解
### 大事件 1：名称（第 X–Y 章）
- 高潮：第 X 章，……
#### 子事件 1
- 起：
- 承：
- 转：
- 合：

## 可复用创作模板
### 金手指公式
### 主角公式
### 单元事件节奏
### 升级阶梯
### 角色配置
### 爽点公式
### 避坑清单

## 主角成长弧
### 一句话成长弧
### 明线 / 暗线推进
### 核心张力
### 未完成弧线

## 证据与缺口
- 已覆盖章节：
- 未识别或未分析章节：
- 关键结论证据位置：

## 来源信息
- 原文文件：
- 原文哈希：
- 拆解模板版本：
~~~

报告是可编辑项目文件，工作台提供“打开 Markdown”入口。用户编辑后的报告不得被后台刷新静默覆盖；重新拆解先生成差异预览，可选择保留人工段落、覆盖模型段落或另存新版本。

#### 融梗与蒸馏的准入

- `蒸馏`：只允许选中一本 `ready` 且原文 hash 可读的书；报告存在但原文缺失时禁用，并提供“重新关联原文”。
- `融梗`：继续要求至少 3 本 `ready` 书籍。后端再次按 status、报告版本和来源 hash 校验，不能只相信前端勾选结果。
- 融梗输入优先使用各书报告中的结构化事件、设定、成长弧和可复用模板，不直接拼接原文；输出继续作为候选方案，写入当前项目仍需预览确认。
- 重新拆解使书籍进入 `stale/analyzing` 后，从当前融梗选择中自动移除，并显示原因。

#### 融梗应用到大纲、蒸馏应用到风格库

两项功能统一采用“候选产物 → 目标预览 → 用户确认 → 领域服务提交”的流程，但各自保留独立源文件，便于追溯和重新应用。

**融梗：**

1. 生成结束只保存 `融梗方案/<fusionId>/融梗候选.md` 和来源 manifest，状态显示“候选方案，尚未进入项目大纲”。
2. 结果页提供“应用到大纲”，先让用户选择：新建故事大纲草稿、合并到现有故事大纲、生成传统大纲文件，不能根据 `output_mode` 自动覆盖。
3. 选择结构化故事大纲时，将候选解析为 StoryPlanning patch，预览新增/修改的目标、冲突、情节点和章节范围；确认后由 `StoryPlanningService` 更新 `story-planning.jsonl` 与 `故事大纲.md` 投影。
4. 选择传统大纲时，目标固定为 `01_大纲/大纲.txt`，走 generated cache 差异预览；不得直接写 `细纲.txt/章纲.txt`，除非用户重新选择目标。
5. 提交成功后在融梗 manifest 记录 `applied_target`、目标 revision/hash、应用时间；候选文件继续保留，不因应用而移动或删除。

**蒸馏：**

1. 蒸馏结束先保存可追溯档案和一份 `style` 领域草稿，状态显示“蒸馏完成，尚未应用到风格库”。
2. 用户点击“应用为当前文风”后，预览将写入的“写作风格、风格示例、参考素材”区块和与现有风格库的差异，可选择合并或替换。
3. 确认后由 `ProjectLibraryService` 写 style 结构化主数据并同步 `00_设定集/风格库/*.txt` 投影；`.agent/style_distillation/current.json` 继续保存 bookId、原文证据和启停状态，不充当风格库主数据。
4. 上下文构建改为读取“已提交风格库 + 已启用蒸馏证据”，按预算合并；不再因为存在蒸馏档案就完全忽略普通风格库。
5. 未确认应用时，蒸馏档案可预览但默认不宣称“风格库已更新”；若产品允许临时试用，必须明确显示“仅本次试用，未进入风格库”。

“提取写作方法”和“生成迁移预览”复用同一个 style 草稿卡，不再只创建隐藏 `library-drafts` 文件。该草稿必须同时出现在拆书页和“风格与题材”页，确认一次后两处同步为已应用。

#### 蒸馏原文注入

原文注入分为两个阶段，二者都必须实现：

1. **建立蒸馏档案时**：服务端根据 `source_book_id` 读取该书 manifest 和完整原文，按开篇、前期、中期、后期以及对白/动作/环境/心理等场景分层取样；分批提取文风证据后再汇总档案。删除“只截取前 6 万字符”和“缺原文便用拆书产物兜底”的静默行为。
2. **启用蒸馏参与创作时**：项目上下文除 `profile_text` 外，再从同一本原文按本轮任务选取有代表性的受限片段，作为“蒸馏原文证据”注入模型。正文续写偏向相邻叙事与对白证据，大纲任务偏向节奏和章末钩子证据；不把整本小说无差别塞入每次请求。

`StyleDistillationProfile` 增加 `source_book_id`、`source_hash`、`source_report_path`、`evidence_spans`、`evidence_version`。`evidence_spans` 只记录章节、偏移、用途与片段 hash，原文仍以书库文件为唯一来源，避免在 JSON 中复制整书。

每次构建上下文时验证 bookId、路径与 hash：

- 一致：注入蒸馏档案 + 预算内原文证据，并在助手上下文清单显示“蒸馏原文 N 段 / 第 X、Y…章”。
- 原文被修改：标记档案 `stale`，停止以“蒸馏使用中”名义静默生成，提供重新蒸馏或停用。
- 原文丢失：不退回拆书摘要冒充原文；保留档案供查看，但禁用需要蒸馏原文的创作请求并给出重新关联入口。
- 模型上下文预算不足：先减少证据段数并保留全书阶段覆盖，再压缩单段；不得永远只保留开头。

## 5. 建议实施顺序

### 阶段 A（P0）：修复确认状态与假保存

1. 引入 PendingWriteTransaction / system receipt 契约，兼容现有 requires_confirmation。
2. 修复 SkillRunner、Runtime、durable replay 和智能技能编排对确认状态的硬编码覆盖。
3. AI 助手接入 generated cache、library draft 和 Agent confirmation 三类事务。
4. 拦截待确认状态下的纯“确认”输入，统一调用真实 commit。
5. 保存后做路径/revision/hash 读回校验；失败保留事务，不显示假成功。
6. 先用截图中的章纲、风格与题材三个场景做桌面回归。

### 阶段 B（P0）：统一结构化主数据与页面刷新

1. 把“故事大纲/情节点”写入路由到 StoryPlanningService，不再只写旧 大纲.txt。
2. 在 SourcesProductPage、StyleProductPage 补回资料草稿审核与重启恢复。
3. 确认后刷新结构化主数据、投影、页面、文件树、索引和 AI 上下文。
4. 对批量、迁移、拆书、封面、审阅等其他写入入口执行系统回执审计和防回归测试。

### 阶段 C：思考与上下文数据契约

1. 增加 reasoning_enabled、思考内容字段和双通道流事件。
2. 扩展模型流解析器，保持旧正文流接口兼容。
3. 修复结构化故事大纲与稳定上下文的文件来源。
4. 补模型解析、会话存储和上下文拼装单元测试。

### 阶段 D：统一模型入口与助手流式体验

1. 实现“会话选择 + 全局默认”一致保存。
2. 从设置页移除文本语言模型选择。
3. Controller 分离思考流与正文流。
4. 增加思考开关、思考面板、阶段状态和无思考内容提示。
5. 加固停止、断流、非流式回退、去 AI 味二阶段状态，并更新自动项目上下文显示。

### 阶段 E：项目入口与大纲 UI

1. 新项目创建成功后进入 AI 助手，覆盖直接和二次确认流程。
2. 修复情节点行网格。
3. 增加中间区就地编辑、排序、删除和保存状态。
4. 完成不同窗口宽度下的结构调整。

### 阶段 F：正文侧栏独立迷你对话

1. 抽出按显式 conversationId 创建、加载和流式发送的会话控制能力，禁止自动切换页面。
2. 为正文侧栏建立独立会话、输入草稿、消息流、停止状态与写入事务状态。
3. 替换旧“最后一条全局 AI 回复”区域，渲染当前侧栏会话完整紧凑线程。
4. 保留三个快捷操作，增加当前段落解析与“扩写当前段落”。
5. 补章节切换、项目切换、打开完整对话、响应式抽屉和辅助功能测试。

### 阶段 G：拆书状态流、Markdown 报告与蒸馏原文

1. 扩展拆书 manifest 与迁移器，导入只创建 `imported` 记录，修复拆解重复 bookId 和 legacy 全局覆盖。
2. 改造长篇导入、章节索引、分块拆解、checkpoint、覆盖率校验与 `拆书报告.md` 汇总。
3. 重排拆书工作台为待拆队列/正式书库和单一动作面板，只让 `ready` 书籍进入蒸馏或 3 本融梗。
4. Nuwa 按 `source_book_id + source_hash` 强制读取全书分层原文证据，移除前 6 万字和拆书产物静默兜底。
5. 在后续大纲/章纲/正文生成上下文中注入蒸馏档案与预算内原文证据，并显示可追溯章节清单。
6. 迁移旧拆书目录，补导入、失败恢复、重新拆解、报告人工编辑保护、原文缺失/hash 变化和多入口一致性测试。
7. 为融梗候选增加 StoryPlanning/传统大纲应用事务，为蒸馏增加 style 草稿提交；修复蒸馏替代普通风格上下文的问题。

## 6. 预计涉及文件

前端：

- apps/workbench/src/features/product/editor/EditorProductPage.tsx
- apps/workbench/src/features/product/editor/useEditorAssistantThread.ts（建议新增）
- apps/workbench/src/features/product/editor/editorParagraph.ts（建议新增并单测）
- apps/workbench/src/features/product/outline/OutlineProductPage.tsx
- apps/workbench/src/features/product/home/HomeProductPage.tsx
- apps/workbench/src/features/product/assistant/AssistantProductPage.tsx
- apps/workbench/src/features/product/assistant/AssistantComposer.tsx
- apps/workbench/src/features/product/assistant/AssistantModelControls.tsx
- apps/workbench/src/features/product/sources/SourcesProductPage.tsx
- apps/workbench/src/features/product/style/StyleProductPage.tsx
- apps/workbench/src/features/product/disassembly/DisassemblyProductPage.tsx
- apps/workbench/src/features/disassembly/DisassemblyFeaturePage.tsx（移除旧入口或改为同一领域服务的薄适配层）
- apps/workbench/src/features/project/ProjectSidebar.tsx
- apps/workbench/src/features/product/shared/TutorialDialog.tsx
- apps/workbench/src/features/product/shared/AiWriteTransactionCard.tsx（建议新增共享组件）
- apps/workbench/src/features/settings/SettingsFeaturePage.tsx
- apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts
- apps/workbench/src/lib/workflow.ts
- apps/workbench/src/styles/assistant.css
- apps/workbench/src/styles/editor-assistant.css（建议从巨型 styles.css 抽离）
- apps/workbench/src/styles/disassembly.css
- apps/workbench/src/styles/settings.css
- apps/workbench/src/styles/product-pages.css
- apps/workbench/src/styles/components.css
- apps/workbench/src/App.tsx（仅在需要统一创建完成导航回执时）

共享契约与服务：

- packages/shared/src/schemas/conversation.ts
- packages/shared/src/schemas/agent.ts
- packages/shared/src/schemas/project.ts
- packages/model-client/src/openai-compatible.ts
- packages/agent-runtime/src/chat-runner.ts
- packages/agent-runtime/src/skill-runner.ts
- packages/agent-runtime/src/runtime.ts
- packages/agent-runtime/src/generated-save-planner.ts
- packages/agent-runtime/src/workflows/disassemble-book.ts
- packages/agent-runtime/src/workflows/disassemble-library.ts
- packages/agent-runtime/src/workflows/disassembly-report.ts（建议新增）
- packages/agent-runtime/src/workflows/book-fusion.ts
- packages/agent-runtime/src/workflows/nuwa-style-distill.ts
- packages/project-session/src/service.ts
- packages/conversation-service/src/service.ts
- packages/document-service/src/story-planning-service.ts
- packages/document-service/src/project-library-service.ts
- packages/api-client/src/client.ts
- apps/desktop-shell/src/main/runtime/conversation-routes.ts
- apps/desktop-shell/src/main/runtime/agent-routes.ts
- apps/desktop-shell/src/main/runtime/story-planning-routes.ts
- apps/desktop-shell/src/main/runtime/project-library-routes.ts
- apps/desktop-shell/src/main/runtime/job-routes.ts

测试将优先追加到相邻现有测试文件，不另建无必要的测试框架。

## 7. 验收标准

### 7.1 大纲 UI

- 红圈情节点文字不再竖排、重叠或被压缩到窄列。
- 中间区可以修改标题、状态、摘要、关联章节并保存。
- 可以删除叶子情节点；有子节点时必须明确选择“提升子节点”或“整棵删除”。
- 删除取消不改变数据；删除成功后选择、order、revision 和版本历史正确更新。
- 保存失败保留输入，不丢草稿。
- 上移、下移和拖动排序结果一致；键盘可操作。
- 1920×1080、1440×900、1280×720、1024×720 下无文档级横向滚动。

### 7.2 新项目落点

- 直接创建成功后地址为 /assistant。
- 有未保存内容时，二次确认创建成功后同样进入 /assistant。
- 取消或失败不跳转。
- 打开已有项目不被误判为新建。

### 7.3 模型入口

- 设置页不再出现主语言模型、网站语言模型和副模型选择控件。
- Embedding 与封面生图模型仍可配置。
- 助手选择模型后，当前会话立即使用该模型。
- 重新打开应用后，全局默认仍是最近一次在助手中显式选择的模型。
- 大纲完善、批量生成、审阅等后续 AI 请求使用该全局默认。

### 7.4 思考与流式输出

- 支持思考的模拟模型先流式返回思考，再流式返回正文，两部分显示在各自区域。
- 思考内容不会混入最终正文或写入正文文件。
- 关闭思考后不发送思考参数、不显示思考区。
- 模型不返回思考字段时有准确提示，不报错、不伪造。
- 普通仅正文模型继续兼容流式输出。
- 中途停止后保留已显示部分，状态明确。
- 服务端忽略流式参数时能回退到完整响应。

### 7.5 项目上下文

- 在大纲 UI 保存一条唯一文本后，下一次 AI 请求的模型输入中能找到该文本。
- 人物设定、体系设定、地图设定、道具设定仍能进入稳定上下文。
- 风格、题材、最近正文、固定文档和附件链路不回归。
- 助手右栏准确区分自动项目上下文和本次额外上下文。
- 上下文清单只记录来源与规模，不泄露密钥或复制完整敏感内容。

### 7.6 预览、确认与真实写入

- AI 生成章纲并要求保存时，助手必须显示实际内容预览、规范目标 01_大纲/章纲.txt 和确认/丢弃按钮。
- AI 生成风格或题材时，助手显示资料草稿预览；用户也能在“风格与题材”页找到同一草稿，重启应用后仍存在。
- AI 提取设定时，助手和“设定资料”页都能处理同一 lore 草稿。
- 用户确认前，结构化主数据和正式投影不变；确认后两者在同一事务中更新。
- 待确认时输入纯“确认”与点击按钮执行同一事务；该输入不会出现在模型请求中。
- 模型自行输出“已保存”不会产生系统成功样式，也不会改变文件。
- 系统只在 commit API 成功、路径/revision/hash 读回一致后显示保存回执。
- 故事大纲生成确认后，当前大纲页面立即出现新内容，下一次 AI 请求也能读取；不得只更新 大纲.txt。
- 风格/题材/设定确认后，新版资料页面立即刷新，TXT 投影与 JSONL 主数据一致。
- 确认失败、版本冲突、投影漂移和应用重启均不会丢失预览内容，也不会错误显示成功。
- 批量生成继续只进入批次草稿；素材迁移继续先预览再提交；封面和审阅报告的成功提示均有真实返回路径或 revision 支撑。

### 7.7 正文侧栏迷你 AI 对话

- 打开正文编辑页时，侧栏为空白的新对话，不显示完整 AI 助手当前会话的历史回复。
- 在侧栏发送消息后仍停留在正文编辑页，用户消息与 AI 流式回复都在侧栏内出现。
- 侧栏输入草稿与完整 AI 助手输入草稿互不覆盖。
- 侧栏会话出现在项目会话列表；点击“在 AI 助手中打开”加载同一 conversationId 和完整消息，不创建副本。
- 收起再展开、同一项目内切换章节不会无故创建新会话；点击“新对话”才新建；切换项目不会复用旧项目会话。
- 新对话跟随当前全局默认模型，正文侧栏没有额外模型选择控件。
- 续写当前段落、检查人物口吻、增强悬念三个快捷操作的文案、顺序和填充输入框行为保持不变。
- 新增“扩写当前段落”；有选区时使用选区，无选区时使用光标所在段落，空白位置有明确提示。
- 扩写请求携带准确的 documentPath、range、原文和 revision/hash，模型不需要从整章猜测目标段落。
- 生成结果不会自动覆盖正文；要求写入后显示原段/新段预览，文档变化时阻止旧范围覆盖。
- 思考、正文、停止、错误和待确认写入都能在窄侧栏正确显示。
- 1920×1080、1440×900、1280×720 下侧栏与正文均可用；窄窗口改用右侧抽屉且无页面级横向滚动。

### 7.8 拆书、Markdown 报告、融梗与蒸馏

- 导入一本小说后只出现一个 `imported` 待拆条目；开始、停止、恢复、失败重试和重新拆解始终复用同一 bookId，不产生同名副本。
- 待拆条目不会出现在正式拆书库和融梗/蒸馏可选列表；只有报告完整且读回校验通过的 `ready` 条目才进入。
- 1–489 章这类长篇不会被 20 万字符静默截断；界面显示总章节、已分析章节、缺失章节和覆盖率。
- 每本 `ready` 书籍都存在可打开的 `拆书报告.md`，包含核心设定、逐章速览、黄金三章与前十章、大事件起承转合、可复用模板、成长弧、证据缺口和来源元数据。
- 报告是标准 Markdown，不依赖 Notion 私有标记；工作台可折叠预览，外部编辑器打开仍可读。
- 人工修改报告后，重新拆解先显示差异，不静默覆盖；确认后 manifest 的报告版本与文件 hash 一致。
- 单书 `ready` 可蒸馏；融梗至少选择 3 本 `ready`，后端拒绝部分产物、stale、原文缺失或 hash 不一致的伪完成条目。
- 融梗生成完成后只保存候选，不自动修改大纲；点击“应用到大纲”能预览并明确选择结构化故事大纲或 `01_大纲/大纲.txt`，确认后目标页面和 AI 上下文立即更新。
- 蒸馏生成完成后能看到待应用的 style 草稿；确认后结构化风格主数据和 `风格库/*.txt` 投影同步更新，风格页面立即可见。
- 蒸馏启用后，普通风格库与蒸馏证据按预算合并进入上下文，不再二选一；未确认草稿不得显示“风格库已更新”。
- 创建蒸馏档案时，模型输入含来自全书早/中/后段及多种场景的原文证据，不能只包含前 6 万字或拆书摘要。
- 启用蒸馏后的大纲、章纲与正文请求同时注入文风档案和预算内原文证据；助手上下文清单可看到片段数和章节来源，原文内容不混入最终正文文件。
- 删除、改动或替换原文后，蒸馏档案标记 stale 并提示重新关联/重新蒸馏，不得静默用拆解产物冒充原文继续生成。
- 上传、联网爬取、产品工作台、项目树侧栏和任何保留的旧入口均使用相同状态机、完成判定与错误文案。

## 8. 测试建议

至少增加以下自动化用例：

1. openai-compatible：交错接收 reasoning_content 与 content，顺序和通道正确。
2. openai-compatible：只返回正文、非流式 JSON、服务端忽略 stream=true 均兼容。
3. chat-runner：思考与正文分别持久化，思考不进入写回内容。
4. chat-runner：结构化 故事大纲.md 和设定投影确实进入模型消息。
5. conversation-service：旧会话缺少新字段时默认值正确。
6. api-client：NDJSON 的 reasoning/answer 事件按顺序分发。
7. useWorkbenchCoreController：两路增量累加、停止、最终替换正确。
8. UI/E2E：新项目创建后进入助手。
9. UI/E2E：助手选择模型后设置持久化为全局默认，设置页无文本模型选择。
10. story-planning-service：删除叶子节点、提升子节点、整棵删除、revision conflict 和版本回滚。
11. UI/E2E：大纲中间区编辑并保存中文标题、摘要和关联章节；删除及取消删除行为正确。
12. skill-runner/runtime：library_draft 的 requires_confirmation 透传到 AgentRunResponse，普通与 durable/stream/replay 结果一致。
13. runtime 智能编排：任一步产生待确认事务时最终响应保留事务，不被后续步骤覆盖。
14. controller：generated_cache、library_draft、agent_confirmation、story_planning_patch 四类事务都能恢复和提交。
15. controller：存在一个待确认事务时输入“确认”调用 commit 且不发送模型；多个事务时要求选择。
16. assistant UI：事务卡显示完整预览、规范目标、风险、按钮、提交中/冲突/失败/成功状态。
17. project-library：style/genre/lore 草稿确认后 JSONL 主数据和全部 TXT 投影原子更新，失败不归档草稿。
18. Product UI：风格、题材、设定草稿在目标页面可见，确认/丢弃后刷新，应用重启后可恢复。
19. 领域路由：“故事大纲”写入 StoryPlanningService，“章纲”写入 章纲.txt，模型虚构的 章纲.md 不会成为成功目标。
20. 系统回执：只有经过读回校验的结果生成 committed receipt；模型文本不能伪造 receipt。
21. 全局回归：批量生成、素材迁移、拆书、封面、审阅报告和技能编辑保留各自安全写入语义。
22. editorParagraph：选区优先、LF/CRLF、空行分段、一段一行、文首文末、空白段回退和全空文档。
23. editor sidebar controller：创建会话不改变 activeTab，不修改全局 conversationDetail/messageInput。
24. editor sidebar controller：按显式 conversationId 流式发送、停止、错误恢复和双通道增量正确。
25. editor sidebar controller：章节切换更新 current_path，项目切换清除旧会话，收起展开不重复创建。
26. EditorProductPage：旧全局 latestAssistantMessage 不再渲染；新线程显示 user/assistant/system 消息与空态。
27. UI/E2E：三个旧快捷操作行为不变；扩写按钮绑定选区/当前段落且只填充侧栏输入框。
28. UI/E2E：从侧栏打开完整 AI 助手复用同一会话，返回正文后侧栏线程仍可继续。
29. disassemble-library：导入完整原文只创建一个 imported manifest，字符数、章节数、hash 与文件读回一致。
30. disassemble-book：传入已有 source_book_id 原地推进状态，重复点击、重试和 checkpoint 恢复都不创建第二个 bookId。
31. disassemble-book：imported → analyzing → ready/failed/stale 状态转换合法；部分产物或报告校验失败不能标记 ready。
32. 长篇导入：超过 20 万字符的测试小说完整保存并跨首、中、尾章节拆解；超过产品硬限制时明确失败而非截断成功。
33. disassembly-report：生成标准 `拆书报告.md`，必需标题顺序、章节范围、覆盖缺口、证据位置和来源元数据齐全。
34. DisassemblyProductPage：待拆/正式书库分离，进度/错误/恢复可见，只有 ready 书籍显示蒸馏和融梗入口。
35. 报告版本：人工编辑后重新拆解生成差异预览，取消不改文件，确认后 report hash 与 manifest 同步。
36. book-fusion：少于 3 本、包含 imported/failed/stale、伪造路径或原文 hash 不一致均被后端拒绝；3 本 ready 正常生成候选。
37. nuwa-style-distill：source_book_id 必须解析到 ready manifest 和原文；原文缺失时不得退回当前文档、拆书设定或反向细纲并声称成功。
38. nuwa-style-distill：长篇分层取样覆盖开篇、中段、后段及多种场景，档案保存 evidence_spans 与来源 hash，不再只取前 6 万字符。
39. project-session：蒸馏开启时上下文包含 profile_text 和预算内原文证据；关闭时均不注入；原文变化后标记 stale 并阻止错误“使用中”状态。
40. 兼容迁移：现有拆书目录能推导初始状态；旧 legacy 文件只读兼容，不会在新拆书时被跨书覆盖。
41. UI/E2E：完成“导入 → 待拆 → 拆解 → 打开 Markdown 报告 → 单书蒸馏 / 三书融梗”完整流程，刷新和重启后状态不丢失。
42. book-fusion 应用事务：生成候选不改变大纲；确认 StoryPlanning patch 后主数据、Markdown 投影、revision 和上下文一致，取消保持原状。
43. distillation style 草稿：生成档案不伪报风格库已更新；确认后 style JSONL 主数据和三个 TXT 投影一致，失败保留草稿。
44. project-session 风格合并：蒸馏启用时同时保留普通风格库和蒸馏档案/原文证据，关闭蒸馏后只移除蒸馏部分。

最终验证：

~~~powershell
npm run typecheck
npm test
npm run build:workbench
npm run test:e2e
git diff --check
~~~

涉及桌面流式传输与创建目录选择的用例，再补：

~~~powershell
npm run build:desktop
npm run smoke:desktop
~~~

## 9. 风险与控制

| 风险 | 控制方式 |
| --- | --- |
| 不同中转服务的思考字段不一致 | 以适配器归一化，多种真实样例测试；未知字段不猜测 |
| 某些模型不提供原始思考过程 | 只显示真实返回；无内容时给出能力说明 |
| 新字段导致旧会话无法读取 | 所有新字段 optional/default，保留 passthrough 与迁移测试 |
| 模型选择保存一半成功 | 会话与全局配置统一动作并明确分阶段失败，不显示假成功 |
| 新旧大纲同时存在且冲突 | 结构化投影优先，旧文件只在新投影为空时回退 |
| 模型文本谎称保存成功 | 保存成功只由带 receipt 的系统事件产生，模型消息无权改变事务状态 |
| 顶层确认状态再次被包装器覆盖 | pending_action 由统一函数推导；普通、流式、durable、replay、智能编排共享契约测试 |
| 草稿确认后只更新兼容 TXT | 领域 commit 原子写主数据和投影，并以 revision/hash 读回验收 |
| 用户离开会话后找不到待确认内容 | 事务持久化并在助手、目标资料页和全局待处理入口可恢复 |
| 多个事务时“确认”写错目标 | 仅单一事务允许文字快捷确认；多个事务必须显式选择 |
| 删除父情节点误删子树 | 默认不选择整棵删除，确认区显示影响数量并保留版本回滚 |
| 正文侧栏和完整助手状态互相污染 | 侧栏使用显式 conversationId 与独立 input/stream/error 状态，不通过替换全局状态复用函数 |
| 每次进入正文都制造空会话 | 使用惰性创建并在 Controller 记录本次项目编辑会话 ID，只有首次输入或显式新建时落盘 |
| 当前段落边界识别错误 | 选区优先，兼容换行格式，显示目标摘要；发送时绑定原文、范围和 hash |
| 切换章节后 AI 仍引用旧正文 | 每轮请求读取当前 activeDocument.path，并写入消息上下文切换标记 |
| 窄窗口三栏挤压正文 | 小尺寸默认收起，使用右侧抽屉覆盖而非继续压缩正文 |
| 流式 Markdown 频繁重排 | 帧级合并增量，最终完成后做一次完整渲染 |
| 去 AI 味造成最终文本跳变 | 显示明确的二阶段状态，保留最终文本替换说明 |
| 三栏在窄窗口失去可用性 | 以容器宽度切换三栏/两栏，详情栏渐进披露 |
| 导入与拆解再次生成两份书籍 | bookId 在导入时唯一创建；所有后续工作流必须带 source_book_id 并加书级运行锁 |
| 长篇小说被静默截断 | 完整落盘、章节索引和字符/hash 对账；硬限制在导入前显式报错 |
| 部分拆解文件被误判为完成 | ready 只由报告、覆盖率、必需产物和读回校验共同提交，UI 不自行猜测 |
| 重新拆解覆盖人工修改的报告 | 新报告进入差异预览，按区块合并或另存版本，禁止后台静默覆盖 |
| 蒸馏只学习开头文风 | 按全书阶段和场景分层取样，保存 evidence_spans 并测试尾部章节进入证据集 |
| 原文缺失仍显示蒸馏使用中 | 每次构建上下文校验 source_book_id/path/hash；失配即 stale 并提供重新关联 |
| 注入原文挤占创作上下文 | 为原文证据设独立预算，优先阶段覆盖和任务相关片段，保留来源而不整书注入 |
| 旧拆书入口产生不同状态 | 移除未路由旧页或改为领域服务薄适配；产品页、项目树和任务入口共享后端校验 |
| 融梗生成后用户误以为大纲已更新 | 候选状态明确标“尚未应用”，只能通过 StoryPlanning/大纲写入事务产生系统应用回执 |
| 蒸馏档案与正式风格库分裂 | 生成 style 草稿并由 ProjectLibraryService 提交；档案保留证据，风格库作为正式可编辑主数据 |
| 蒸馏启用后普通风格设置失效 | 上下文改为预算合并并显示来源清单，用回归测试保证普通风格字段仍存在 |

## 10. 完成定义

只有同时满足以下条件，本轮才算完成：

1. 初始六项需求和新增的保存、删除、思考开关问题均有可见结果，不仅是底层字段存在。
2. 思考内容与最终正文从 Provider 到持久化、传输、UI 全程分离。
3. 新大纲 UI 保存的内容能够被下一次 AI 创作请求读取。
4. 文本模型选择在设置页彻底消失，助手最近选择成为全局默认。
5. 新建项目的直接流程和二次确认流程都进入 AI 助手。
6. 故事大纲情节点可安全删除，子节点策略、冲突和回滚均有覆盖。
7. 章纲、故事大纲、设定、风格、题材的 AI 写入都能看到真实预览，确认后更新正确的主数据和页面。
8. 软件中所有 AI 写入成功提示都来自经过读回验证的系统回执，模型文本不能制造假成功。
9. 正文侧栏使用独立新会话，能够在不离开正文页的情况下完成多轮流式对话，并可在完整 AI 助手中继续同一会话。
10. 三个原快捷操作保持不变，“扩写当前段落”能稳定定位选区/光标段落且不会未经确认覆盖正文。
11. 拆书严格遵循“导入待拆 → 原书拆解 → 完整报告入库 → 选择蒸馏或三书融梗”，不再产生重复书籍或伪完成状态。
12. 每本正式入库书籍具有符合参考笔记信息结构的 `拆书报告.md`，长篇覆盖和人工修改保护均有验证。
13. 蒸馏建立和后续使用都能注入可追溯的原文证据；原文丢失或变化时不会静默降级冒充成功。
14. 融梗候选经确认后能进入正确的大纲主数据；蒸馏草稿经确认后能进入正式风格库，且两者未确认前都不改变正式资料。
15. 自动化测试、桌面冒烟和目标分辨率截图验收通过。
