# ArcWriter 可用性迁移实施方案

## 目标

保留 ArcWriter 的小说项目、设定、时间线、拆书、技能和纸面风格界面；只吸收 OpenCode 已被验证的会话工作模式，使 AI 助手能持续执行、看得见过程、可恢复，并且文件操作只作用于当前项目。

## 迁移范围

### 会迁移

- 消息按部件保存：正文、可展示推理、附件、技能、工具调用、执行步骤、重试和错误独立持久化。
- 模型主导的工具循环：模型先选择受限工具，再读取项目资料、调用技能或执行项目文件操作；每轮工具结果回到同一上下文，直到任务完成。
- 按需加载技能：始终只提供技能名称和简介，模型确定调用后才读取完整技能说明。
- 失败重试和上下文压缩：保留最近对话、用户约束、当前任务和已完成工具结果，避免长任务因单轮输出或超时直接结束。
- 对话输入体验：附件卡片、拖放/粘贴、`/` 技能入口、`@` 项目文件引用、可停止的流式输出和非强制自动滚动。

### 不迁移

- OpenCode 的 Bun/Effect/Drizzle 服务端、CLI/TUI、账号体系、Git 工作树、Shell、LSP、MCP、插件市场和供应商管理。
- Cherry Studio 或 OpenCode 的整体界面、主题、侧栏和项目模型。

## 文件安全规则

所有读写均由主进程以当前项目根目录为边界校验真实路径；路径穿越、符号链接逃逸、版本冲突和内容校验失败一律拒绝。

- 创建、追加、局部修改、合并和自动提取设定：直接执行并记录时间线。
- 覆盖已有文件：显示真实差异后确认。
- 删除：仅移入项目 `99_回收站`，确认后原子执行且可从时间线恢复。
- 没有明确要求保存的生成内容：仅生成预览，不写项目。

## 实施顺序

1. 完成消息部件的兼容存储和流式状态恢复。
2. 将当前会话请求接到受限工具循环，保留旧路由作为回退。
3. 将技能、项目文件与拆书工作流注册为受限工具，并实现检查点/重试。
4. 完成附件、`/` 与 `@` 输入体验，继续使用现有 ArcWriter 视觉规范。
5. 收紧文件确认规则并移除全局“直接保存删除”开关。
6. 为拆书、工具循环、文件边界、覆盖/删除确认、流式恢复添加单元、接口和桌面冒烟测试。

## 验收标准

- 一条复杂请求可持续经过“理解 → 读取 → 调用技能/工具 → 校验 → 回复或写入”，每一步可见且重开软件仍可恢复。
- 对话流式输出不中断；模型输出未完成时自动续写或明确报出可恢复失败原因。
- 附件和项目引用实际进入模型上下文，不是仅有界面显示。
- 除覆盖和回收站删除外，明确的项目内文件操作无需确认；任何项目外路径均无法操作。
- 现有小说功能、项目文件格式和纸面风格界面保持不变。
