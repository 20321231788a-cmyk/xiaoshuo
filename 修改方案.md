# ArcWriter 全量 UI 二次重构修改方案

> 目标：让接手 AI 能直接使用本文中的提示词，逐页把当前功能界面重构为 `output/ui-final` 设计稿中的产品界面。设计稿决定布局、层级、密度和交互表达；现有代码只提供真实数据、业务能力和兼容逻辑。

## 1. 本轮结论

当前版本的问题不是颜色或间距没有微调，而是多数导航点击后仍进入旧组件结构：文件树、原始 JSON、开发式历史记录、通用表单、旧卡片网格和大段纵向设置页仍在主导页面。上一轮只替换了 AppShell 和部分页面外框，没有把 17 张设计稿对应的页面骨架逐一实现。

本轮必须遵守以下判断标准：

1. 页面结构与设计稿不同，即视为未完成，不能用“功能已经接入”代替 UI 还原。
2. 真实数据为空时使用设计稿同风格的空状态，不得用演示数据填充，也不得退回旧页面。
3. 旧组件可以保留为数据和行为来源，但不得直接整页嵌入新页面。
4. 普通用户界面不得出现原始 JSON、JSONL、哈希、日志、Trace、IPC、终端、向量数值、内部路径回执或执行步骤。
5. “抽卡”只出现在 AI 助手和批量章节生成中，不建立独立导航页。
6. “技能查看”必须保留，但属于“创作工具”的三级页面。
7. “AI 配置”必须保留，但属于“设置”的二级页面。

## 2. 唯一视觉基准

- 页面设计稿：`output/ui-final/arcwriter-01-home.png` 至 `output/ui-final/arcwriter-17-states.png`
- 总览图：`output/ui-final/arcwriter-ui-suite-overview.png`
- 交互原型结构：`output/ui-final/src/main.tsx`
- 原型样式：`output/ui-final/src/styles.css`
- 产品上下文：`PRODUCT.md`

原型中的示例书名、章节、数字和人物只用于说明布局。实现时必须连接真实项目数据；真实数据为空时显示空状态。

## 3. 固定信息架构

| 层级 | 页面 | 说明 |
| --- | --- | --- |
| 一级 | 写作、规划、资料、审阅、工具 | 仅作为主导航分组 |
| 二级 | 项目首页、正文编辑、AI 助手、故事大纲、伏笔与时间线、设定资料、风格与题材、小说编辑室、全文审阅、项目记忆、拆书工作台、批量章节生成、素材迁移、创作工具 | 左侧主导航可见页面 |
| 二级 | 后台任务、设置 | 固定在左侧导航底部 |
| 二级 | AI 配置、写作体验、项目与备份、隐私与数据、快捷键、关于 | 只在“设置”内部的二级导航中出现 |
| 三级 | 技能查看、技能编辑、技能版本历史、技能导入预览 | 只能从“创作工具”页进入，不出现在主导航 |
| 非导航 | 关键交互状态 | 作为共享状态组件和验收页面，不进入生产导航 |

生产路由要求：

- `/tools`：创作工具列表。
- `/tools/skills/:skillId`：技能查看三级页面。
- `/tools/skills/:skillId/edit`：技能编辑三级页面，可与查看页使用同一页面状态切换。
- `/settings/ai`：设置内的 AI 配置二级页面。
- `/settings/writing`、`/settings/backup`、`/settings/privacy`、`/settings/shortcuts`、`/settings/about`：设置内其他二级页面。
- 直接访问三级或二级地址时应保持正确的左侧导航选中状态和面包屑，不新增一级导航项。

## 4. 接手 AI 的使用方法

每次只改一个页面。将“全局母提示词”和该页面的“页面提示词”一起交给接手 AI。完成后先跑该页浏览器验收，再进入下一页。禁止一次性用一套通用布局批量覆盖所有页面。

建议执行顺序：

1. 全局 AppShell 和共享组件。
2. 正文编辑、AI 助手。
3. 故事大纲、伏笔与时间线。
4. 设定资料、风格与题材。
5. 小说编辑室、全文审阅、项目记忆。
6. 拆书、批量生成、素材迁移、创作工具。
7. 后台任务、设置、关键状态。

## 5. 全局母提示词

```text
你正在重构 ArcWriter Windows 桌面端。请先读取 PRODUCT.md、output/ui-final/README.md、目标页面 PNG、output/ui-final/src/main.tsx 和 output/ui-final/src/styles.css，再读取当前页面组件与 controller/API。目标页面 PNG 是布局、层级、密度、配色和控件形态的唯一视觉基准，当前页面只作为真实数据与行为来源。

必须保持真实业务能力和向后兼容，不得用静态演示数据替代真实数据。数据为空、加载、失败、无权限和能力不可用时，使用与 arcwriter-17-states.png 一致的状态组件。不得为了沿用旧组件而改变设计稿布局。

全局壳体固定：210px 主导航、62px 顶栏、暖象牙内容面、冷灰导航面、深墨文字、低饱和茄紫强调色。正文使用稳定中文宋体，其他 UI 使用系统无衬线字体。按钮和输入框圆角 4-6px，独立面板 7-8px，边框 1px。强调色只用于当前选择和主要动作。动效 150-220ms，并支持 prefers-reduced-motion。

主导航只显示产品页面，不显示日志、Agent Trace、终端、向量测试、哈希、IPC、内部执行记录、技能 ID、handler_type 或原始文件回执。禁止把旧 xw-feature-page、旧 OperationsView、旧 RightRail 或旧页面整块嵌进新页面。可以复用其 hooks、请求函数、controller 方法和纯业务组件。

所有 AI 写入统一遵循：生成结果 -> 差异预览 -> 用户确认 -> 创建版本记录 -> 写入。任何页面不得直接覆盖正文。抽卡只在 AI 助手和批量章节生成内出现，候选数 2-4。

实现完成后用真实浏览器在 1280x720、1440x900、1024x720 验证：无文档级横向滚动；文本不截断或重叠；键盘焦点清晰；主导航完整；弹出层不遮挡主要操作。截图与目标 PNG 并排比对。若骨架、栏宽、信息层级或主要控件位置不同，继续修改，不得宣布完成。
```

## 6. 逐页实现提示词

### 01 项目首页

参考图：`output/ui-final/arcwriter-01-home.png`

```text
重构“项目首页”，严格还原 arcwriter-01-home.png。不要保留当前通用首页卡片结构。

页面顶部是紧凑欢迎区：左侧显示问候、当前目标和继续写作提示；右侧是“打开项目”和“新建小说”。主内容第一行是横向继续写作面板，包含真实封面缩略图、当前章节、上一句摘要、写作进度条、“继续写作”“询问 AI”以及总字数、章节数、本周字数、待处理记忆四项数据。下方使用 2:1 布局：左侧“最近项目”为紧凑列表，右侧“今日写作”为 7 日柱状图和连续写作状态。

接入真实项目、新建、打开、导入、最近项目、最近章节、目标字数和本地保存状态。没有项目时，不显示虚假的书名和统计，改为设计系统空状态，主操作为“新建小说”，次操作为“打开项目”。请求失败显示可重试错误状态。

验收重点：首屏必须同时看到继续写作、最近项目和今日写作；不得出现 SaaS 指标卡网格；不得出现项目文件树、运行日志或内部路径详情。
```

### 02 正文编辑

参考图：`output/ui-final/arcwriter-02-editor.png`

```text
重构“正文编辑”，严格还原 arcwriter-02-editor.png。当前截图中的“项目文件与资料卡”、原始 .agent 文件、保存回滚日志和大块旧编辑面板全部从正文主界面移除。

在 210px 主导航右侧建立三段工作区：208px 章节栏、中央正文编辑区、318px 可折叠 AI 侧栏。章节栏只展示卷、章节、搜索、新建章节和章节保存状态，不展示设定目录、JSONL 或所有项目文件。中央区顶部是固定编辑工具栏，包含撤销、基础格式、标题样式、保存状态、字数和更多菜单；正文纸张居中，正文行宽控制在 65-75ch，章节标题清晰，长时间写作不疲劳。AI 侧栏显示当前上下文 chips、一次建议预览、采用建议/生成新版本，以及续写、检查人物口吻、增强悬念等快捷动作。

保留真实文档打开、编辑、查找替换、保存守卫、保存冲突、版本回退和多标签脏状态。AI 侧栏可收起；1024px 时默认收起，章节栏仍可用。保存成功只显示简短状态，不在正文下面展示版本日志。版本历史通过工具栏菜单进入独立抽屉或页面。

验收重点：正文是最大、最亮、最安静的区域；打开任意章节后首屏不得出现文件树详情卡、原始 JSON 或历史记录列表；编辑器与设计稿栏宽和层级一致。
```

### 03 AI 写作助手

参考图：`output/ui-final/arcwriter-03-assistant.png`

```text
重构“AI 写作助手”，严格还原 arcwriter-03-assistant.png。不要复用当前“会话列表 + 会话详情表单 + 固定上下文表单”的旧双卡页面。

页面采用三栏：约 206px 会话栏、可伸展对话区、约 250px 本次上下文栏。会话栏顶部是“新建对话”和搜索，下面按今天/昨天分组。中间区显示会话标题、消息流、AI 建议改写块，以及“插入正文”“与原文对比”“重新生成”三个明确动作；底部是粘性输入区。右栏显示 AI 本次会读取的章节、证词、人物、风格规则，每项可移除，可添加资料，并显示“写入范围”安全说明。

在会话标题区提供“对话/抽卡”分段控件。抽卡模式在中间区并排显示 2-4 个候选，支持查看差异、采用一版、保留其余草稿；采用后仍必须进入写入确认。上下文选择、附件、会话改名、摘要和停止生成继续连接真实功能，但以设计稿的信息层级呈现。

验收重点：用户一眼能看到当前会话、生成结果和本次上下文；不得显示技能 ID、Agent 计划、Trace、内部阶段名或开发式结果对象。
```

### 04 故事大纲

参考图：`output/ui-final/arcwriter-04-outline.png`

```text
重构“故事大纲”，严格还原 arcwriter-04-outline.png。删除当前只有两个输入框和空白区域的简化实现。

页头显示当前分卷标题、说明、主线/人物线/章节分段控件、“AI 完善大纲”和“添加情节点”。主体是三栏：左侧故事结构导航，显示主线大纲、人物线、伏笔线和分卷；中间显示本卷目标摘要和可拖拽排序的编号情节点列表；右侧是选中情节点详情，包含标题、剧情目标、参与人物 chips、关联章节、检查结果和“保存修改”。

连接 ProjectOutlineBundle 的稳定 ID、父子关系、排序、章节关联和修订号。新增、编辑、排序、切换结构和冲突刷新均操作真实数据。外部文本投影漂移时使用统一冲突状态，不回退到文本输入页。

验收重点：至少可以完成“选择分卷 -> 选择情节点 -> 编辑详情 -> 保存 -> 顺序保持”；真实数据为空时显示“添加第一条主线”的结构化空状态。
```

### 05 伏笔与时间线

参考图：`output/ui-final/arcwriter-05-clues.png`

```text
重构“伏笔与时间线”，严格还原 arcwriter-05-clues.png。默认伏笔视图由页头分段控件切换到故事时间线，不能把文件版本历史称为时间线。

伏笔视图顶部显示“活跃伏笔、已回应、临近章节、故事跨度”四段统计条；右上提供扫描全文和新建伏笔。中部是状态 tabs、筛选、搜索和紧凑表格，列为伏笔、首次出现、计划回应、状态、关联人物、更多操作。planned/planted/due/resolved 使用中文状态并配合文字和颜色。

故事时间线视图使用 StoryTimelineBundle，按故事时间和 sort_key 排序，展示事件摘要、关联章节、人物和伏笔；支持新增、编辑和筛选。全文扫描只显示作者可处理的候选，不展示索引日志。

验收重点：伏笔表格密度、统计条和筛选位置与设计稿一致；切换时间线后数据含义必须是小说故事时间，不得出现 save_document、revision 或内部时间记录。
```

### 06 设定资料

参考图：`output/ui-final/arcwriter-06-sources.png`

```text
重构“设定资料”，严格还原 arcwriter-06-sources.png。禁止继续显示项目目录树、文件卡、原始 JSONL 和保存日志。

页头包含设定搜索和“新建设定”。下方是人物、地点、势力、物品、规则的分类 tabs 和数量。主体两栏：左侧是当前分类的紧凑实体列表与筛选，右侧是选中实体详情。人物详情包含头像/字标、身份、年龄、人物核心字段、关系图和人物弧光时间轴；其他实体沿用同一详情框架并替换领域字段。页头提供“变更”和“编辑”。

连接现有结构化资料库、关系、冲突、修订和文本投影。编辑使用右侧详情的就地模式或同尺寸详情面板，不跳回文件编辑器。变更历史只显示作者能理解的字段差异、时间和恢复操作。

验收重点：选择列表项后右侧即时更新；用户永远不需要看到存储文件名、schema_version、projection_hashes 或 JSON 内容。
```

### 07 风格与题材

参考图：`output/ui-final/arcwriter-07-style.png`

```text
重构“风格与题材”，严格还原 arcwriter-07-style.png。

页头提供导入规则和添加规则。主体三栏：左侧为写作风格、题材规则、范文片段、禁用表达；中间显示当前风格摘要、叙事规则列表和语言偏好 chips；右侧固定“效果预览”，并排或上下显示应用前、应用后以及规则一致性状态。规则列表按视角、节奏、悬念、情绪等分组，可编辑、排序和启停。

连接真实风格库和题材库。导入前显示差异预览；应用预览使用真实文本或用户输入，不自动写入正文。禁用表达必须可搜索、批量导入和说明原因。

验收重点：页面必须同时看到规则与效果预览；不能退化为多份文件列表或通用表单。
```

### 08 小说编辑室

参考图：`output/ui-final/arcwriter-08-studio.png`

```text
重构“小说编辑室”，严格还原 arcwriter-08-studio.png。

使用左右两栏。左栏是编号流程：审阅范围、选择审阅角色（最多 3 个）、本次目标、同意检查相关前后文，底部主按钮“开始运行审阅”。角色使用剧情审阅、人物审阅、连续性审阅、文风审阅等明确作者语言。右栏显示审阅完成摘要，可切换“合并建议/按角色”，每条建议包含严重度、问题、理由、定位原文、生成修改和来源角色；底部提供“逐条处理”和“查看修改预览”。

连接真实多角色审阅结果。默认只显示合并建议；角色意见冲突时明确列出来源和差异，由用户决定。任何建议都不能直接覆盖正文。

验收重点：选择角色和结果列表必须同时处于首屏；不得显示 Agent 名称、prompt、trace 或内部并行运行信息。
```

### 09 全文审阅

参考图：`output/ui-final/arcwriter-09-review.png`

```text
重构“全文审阅”，严格还原 arcwriter-09-review.png。保留现有版本化 ReviewReport 数据，但重新组织视觉结构。

顶部质量摘要由整体分数、剧情推进/人物一致/连续性/文风稳定/章节节奏横向条和优先建议组成。右上是历史报告和开始新审阅。下方左侧为审阅维度及数量、范围选择；右侧为待处理/已采纳/已忽略 tabs、筛选和问题列表。每条问题显示严重度、章节范围、摘要和展开按钮，展开后提供定位原文、生成修改、采纳、忽略。

历史报告切换时保留处理状态；生成修改跳转到差异预览，不直接写入。无项目、无报告、审阅中、任务中断均使用共享状态。

验收重点：评分摘要和问题列表都必须可见；不得只显示一个分数卡和一列简单风险文本。
```

### 10 项目记忆

参考图：`output/ui-final/arcwriter-10-memory.png`

```text
重构“项目记忆”，严格还原 arcwriter-10-memory.png。

页头显示已确认记忆入口和“确认所选 N 条”。顶部信息条说明待确认数量和规则。列表工具栏包含全选、待确认/有冲突/已确认 tabs 和筛选。每条记忆显示选择框、类型标签、事实文本、来源章节与段落、置信判断、状态和更多操作。选中后底部出现粘性确认条，显示已选数量、取消选择和确认并加入记忆。

可确认事实允许批量确认；推测、未来剧情和冲突项禁止批量确认，必须逐条判断、纠正或遗忘。显示作者语言，不显示 embedding、graph claim、source hash 或内部置信计算。

验收重点：选择行为、禁止批量规则和底部确认条必须真实工作；空状态提供扫描项目和手动添加两个动作。
```

### 11 拆书工作台

参考图：`output/ui-final/arcwriter-11-disassembly.png`

```text
重构“拆书工作台”，严格还原 arcwriter-11-disassembly.png。

页头提供导入文本和开始拆解。主体三栏：左侧参考书库、搜索、进度和更多操作；中间显示书籍资料、导出报告、继续分析、总览/黄金开篇/结构节奏/人物体系/世界设定/写作手法 tabs、作品结构摘要和编号方法列表；右侧“应用到当前项目”选择方法、目标位置和生成迁移预览。

连接真实导入、拆解任务、阶段报告、方法提取和迁移预览。拆解中显示进度；失败显示继续/重试；没有报告时显示明确下一步。只显示可供作者使用的分析结果，不展示抓取日志、模型调用步骤或内部文件路径。

验收重点：书库、报告内容和应用面板同时构成完整工作台，不能继续使用旧的操作表单或长结果文本。
```

### 12 批量章节生成

参考图：`output/ui-final/arcwriter-12-batch.png`

```text
重构“批量章节生成”，严格还原 arcwriter-12-batch.png，并在生成方式区域保留“普通生成/抽卡”模式。

左侧主区按步骤组织：1 选择章节，使用表格显示章节、标题、章纲状态、预计字数和编辑入口；2 生成方式，显示按顺序/独立、普通生成/抽卡、每章目标字数、最多尝试、完成后保存方式；3 一致性检查和逐章确认选项。右侧固定生成概览，显示章节数、预计字数、上下文、预计用时、额度上限、保存位置和开始按钮。

抽卡模式每章生成 2-4 个候选，先选候选再进入草稿；未选候选保留在批次结果。默认写入批次草稿，绝不直接覆盖正文。缺少章纲或未确认章纲时明确阻止或提示处理。

验收重点：章节选择和预算概览必须同屏；开始前用户能准确知道生成范围、读取上下文、预计额度和写入位置。
```

### 13 素材迁移

参考图：`output/ui-final/arcwriter-13-transfer.png`

```text
重构“素材迁移”，严格还原 arcwriter-13-transfer.png。

顶部是来源项目与目标项目选择，中间用双向图标表达迁移方向。主体三栏：左侧选择人物设定、世界观规则、文风规则、参考素材及数量；中间是来源内容到目标位置的逐项变更预览，并标记新增、同名冲突；右侧确认迁移，显示来源、目标、新增和冲突数量、两项确认复选框和确认按钮。

冲突处理在中栏就地展开或使用侧栏，不使用默认弹窗。迁移必须排除密钥、会话、额度和内部历史；提交后创建可撤销记录，迁移历史页可执行撤销。

验收重点：双确认未完成时主按钮禁用；用户能在提交前看清每一项去向和冲突处理方式。
```

### 14 创作工具

参考图：`output/ui-final/arcwriter-14-skills.png`

```text
重构“创作工具”，严格还原 arcwriter-14-skills.png。主页面只展示作者能理解并直接使用的工具，不直接展示完整 Skill 技术目录。

页头提供搜索，下面是全部工具、写作与审阅、导入与导出、本地处理 tabs。列表行包含工具图标、名称、作者语言说明、来源/文件访问范围、启用状态和开关或“查看并启用”。页面底部使用绿色安全说明，明确工具没有终端或系统命令权限。

一致性检查、伏笔扫描、离线分词、EPUB/TXT 导入、文档格式转换等映射真实能力。点击属于 Skill 的工具行后，进入“技能查看”三级页面；主导航仍选中“创作工具”。主列表不得显示技能 ID、版本号、handler_type、prompt 摘要、运行/禁用双按钮或分页技术目录。

验收重点：页面外观必须是设计稿中的单列工具清单，不得继续使用当前两列技能卡网格。
```

### 14A 技能查看三级页面

本页没有独立主导航设计稿，视觉语言继承 `arcwriter-14-skills.png` 和设置详情页。

```text
实现“创作工具 > 技能查看 > 技能名称”三级页面。该页面只能从创作工具列表进入，左侧主导航保持“创作工具”选中，不新增“技能查看”“技能目录”或“Skill”导航项。

顶部显示返回创作工具、技能名称、作者语言说明、来源、启用状态和主要动作。主体使用两栏：左侧为用途、输入范围、输出位置、文件访问权限、适用场景和运行入口；右侧为版本、更新说明、启用/禁用、复制为自定义技能和版本历史。只有进入“编辑”状态后才显示 Prompt、上下文要求、关联目标和差异预览。

保留当前 SkillFeaturePage 的导入、本地文件、URL 草稿、复制、编辑、版本、回滚、运行、禁用和恢复能力，但拆成三级页面中的明确任务区。内部 id 和 handler_type 只允许出现在折叠的“技术信息”区域，默认关闭；普通运行结果不得显示内部字段。

所有技能修改必须先预览差异再确认保存。导入技能前显示权限、来源和风险说明。禁止把当前完整 SkillFeaturePage 原样嵌入创作工具主页面。
```

### 15 后台任务

参考图：`output/ui-final/arcwriter-15-tasks.png`

```text
重构“后台任务”，严格还原 arcwriter-15-tasks.png。

页头提供进行中/已完成分段控件。进行中任务使用全宽列表行：图标、任务标题、状态、当前阶段作者语言、进度条、完成数量、预算/上限、预计时间，以及暂停、继续、停止、去确认和更多操作。下方“今天完成”使用更轻的列表，显示结果摘要、耗时和查看结果。

统一 NovelBackgroundTask 和旧任务的 UI 适配。中断、等待确认、预算暂停、退出应用自动暂停都必须有明确状态。不得显示运行日志、Agent Trace、内部 step、命令、原始错误堆栈或任务 payload。

验收重点：生成、审阅、拆书和索引任务在同一套视觉与控制规则下工作；刷新后任务状态可恢复。
```

### 16 设置

参考图：`output/ui-final/arcwriter-16-settings.png`

```text
重构“设置”，严格还原 arcwriter-16-settings.png。禁止继续使用当前所有设置纵向堆叠在一个超长页面的方式。

设置页在 210px 主导航右侧增加约 190px 二级设置导航，包含 AI 配置、写作体验、项目与备份、隐私与数据、快捷键、关于 ArcWriter。右侧主内容只显示当前二级页面。保存按钮位于页头，保存状态即时反馈。

AI 配置是必须保留的设置二级页面，路由为 /settings/ai。页面分为默认写作模型、审阅模型、本地检索三段，字段包括服务商、接口地址、模型、遮蔽 API 密钥、测试连接、审阅模型开关、嵌入模型和检索数量。现有网站 AI、网站模型、联网素材搜索等配置不能删除，可作为 AI 配置内的“网站服务/联网搜索”子分段或 tabs 呈现，但不得再次变成独立主导航页。

其他二级页承接现有写作体验、备份、隐私、快捷键、更新和关于功能。敏感值默认遮蔽；失败信息使用作者可行动语言，不显示 fetch failed、堆栈、请求 URL 或内部诊断。

验收重点：点击设置后先看到二级导航；切换 AI 配置和其他设置不产生整页长滚动；AI 配置功能完整保留。
```

### 17 关键交互状态

参考图：`output/ui-final/arcwriter-17-states.png`

```text
把 arcwriter-17-states.png 落实为共享状态组件，不加入生产主导航。建立 EmptyState、SaveConflictState、AiWriteConfirmState、NetworkRecoveryState、DrawCandidateFailureState 和 TaskInterruptedState。

空状态包含一个主动作和一个次动作；保存冲突显示我的版本、磁盘版本、另存副本、查看差异和保留选择；AI 写入确认显示目标章节、影响范围、可撤销说明、取消、查看差异、确认写入；网络恢复显示已保存草稿数量、稍后处理和重新连接。

这些状态必须被正文编辑、AI 助手、抽卡、故事规划、记忆、任务和迁移实际复用。不得只做一个演示页面。错误文案不得出现 HTTP 状态码、堆栈、接口名或内部步骤。
```

## 7. 代码重构要求

### 7.1 页面边界

将 `apps/workbench/src/features/product/ProductWorkspace.tsx` 继续拆分，最终只保留类型化页面注册和懒加载，不保留具体页面 JSX。建议结构：

```text
features/product/
  registry.ts
  shared/
  home/
  editor/
  assistant/
  outline/
  clues/
  sources/
  style/
  studio/
  review/
  memory/
  disassembly/
  batch/
  transfer/
  tools/
  tasks/
  settings/
```

每个页面负责自己的筛选栏、详情栏、空状态和响应式结构。共享 AppShell 只负责主导航、顶栏和页面插槽。

### 7.2 旧组件处理

- `EditorView`：拆出文档行为 hooks，重做章节栏和正文画布；不再显示项目全部文件和日志历史。
- `ConversationsView`：拆出会话、消息、上下文和写入确认子组件，按三栏重组。
- `ProjectLibraryPages`：保留请求和编辑逻辑，替换为实体列表/详情布局。
- `NovelAgentWorkspace`：拆成 studio、memory、transfer、tasks 独立页面，不再靠隐藏 tabs 复用整页。
- `SkillFeaturePage`：主列表能力移入创作工具，技术性查看/编辑移入三级页面。
- `SettingsFeaturePage`：拆成设置二级路由和分区组件，AI 配置保留为二级页。
- `styles.css` 中旧 `xw-*` 页面样式在新页面完成后删除；新页面不得继续靠覆盖旧 CSS 实现设计稿。

### 7.3 共享组件

建立并统一使用：

- `PageHeader`
- `SegmentedControl`
- `IconButton`
- `PrimaryButton` / `SecondaryButton`
- `SearchField`
- `FilterBar`
- `StatusPill`
- `DataList` / `DataTable`
- `DetailPanel`
- `ContextChip`
- `ProgressBar`
- `StickyConfirmationBar`
- `EmptyState`
- `ErrorRecoveryState`
- `ConflictState`
- `DiffPreview`

所有组件必须具备 default、hover、focus-visible、active、disabled、loading 和 error 状态。

## 8. 实施阶段

### 阶段一：冻结壳体和路由

1. 固定主导航、顶栏、设置二级路由和技能三级路由。
2. 建立页面注册表和共享组件。
3. 将所有旧整页嵌入标记为待替换，禁止新增旧页面依赖。

### 阶段二：写作与规划

1. 正文编辑。
2. AI 助手和抽卡。
3. 故事大纲。
4. 伏笔与故事时间线。

### 阶段三：资料与审阅

1. 设定资料。
2. 风格与题材。
3. 小说编辑室。
4. 全文审阅。
5. 项目记忆。

### 阶段四：工具与设置

1. 拆书工作台。
2. 批量章节生成。
3. 素材迁移。
4. 创作工具主页面和技能三级页面。
5. 后台任务。
6. 设置和 AI 配置二级页面。

### 阶段五：清理与验收

1. 将关键状态接入真实流程。
2. 删除不再使用的旧页面 JSX、RightRail、LegacyWorkbenchView、旧 tabs 和失效 CSS。
3. 检查生产导航中不存在诊断入口。
4. 处理包体和懒加载。

## 9. 每页完成定义

每个页面只有同时满足以下条件才可标记完成：

- 与对应 PNG 的页面骨架、栏宽、层级和主要控件位置一致。
- 使用真实数据；无数据时是设计系统空状态。
- 页面主要功能可完成，不是静态展示。
- 不出现日志、原始 JSON、内部 ID、开发术语或技术错误。
- 高风险动作有差异预览和确认。
- 1280x720、1440x900、1024x720 无横向溢出、遮挡和文本截断。
- 键盘可访问，focus-visible 清晰，正文和表单达到 WCAG 2.2 AA。
- Playwright 截图与设计稿并排核对，并记录差异已经关闭。

## 10. 最终验证命令

```powershell
npm run typecheck -w @xiaoshuo/workbench
npm run typecheck -w @xiaoshuo/desktop-shell
npm run build:workbench
npm run build:desktop
npm run acceptance:ui-library
npm run test:e2e
git diff --check
```

浏览器验收必须覆盖：

- 16 个生产页面逐项点击。
- 技能查看三级页面路由与返回行为。
- 设置 AI 配置二级页面路由与保存行为。
- 正文保存冲突。
- AI 生成差异预览和确认写入。
- 抽卡候选选择。
- 批量草稿和预算暂停。
- 任务暂停/恢复。
- 迁移双确认。
- 空项目、空数据、加载失败和网络恢复。

## 11. 接手 AI 的禁止事项

1. 不得用通用 `ProductPage` 外壳包住旧页面后宣布还原完成。
2. 不得把同一组卡片布局复制到所有页面。
3. 不得把示例数据写进生产代码。
4. 不得为了保留旧组件而显示原始文件结构、JSON 或日志。
5. 不得把技能查看提升为主导航或二级主页面。
6. 不得把 AI 配置移出设置。
7. 不得新增独立抽卡导航。
8. 不得在未完成浏览器截图对比前宣布页面完成。
