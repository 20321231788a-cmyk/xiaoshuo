# ArcWriter 修改计划

## 目标

本轮计划解决以下八项问题：

1. AI 助手在桌面版中仍然一次性显示完整回复，没有真正流式输出。
2. 拆书、正文续写等任务错误复用 AI 助手当前会话，可能造成上下文污染。
3. AI 助手底部状态提示位置不合理，需要移动到输入框工具栏中部的空白区域。
4. “风格与题材”页面的“添加规则”按钮在部分分类下点击后没有可见反馈，看起来没有响应。
5. 向 AI 助手发出“创建故事大纲”“写入大纲”或更新风格、题材、设定资料的指令后，AI 提示完成，但故事大纲、风格与题材、设定资料页面没有相应更新；普通文件写入成功被错误地当成了领域数据保存成功。
6. 故事大纲左侧的分卷区域只有“尚未建立分卷”提示，没有新建入口，现有操作也无法创建分卷。
7. 点击“一键拆解”后缺少即时提示和可见进度，任务可能直接变成“操作已取消”；拆书工作台和章节批量生成都没有可靠的任务进度、失败原因及恢复入口。
8. 设置页仍保留完整的“备用线路”连接配置；需要彻底移除第二套 API，并改为从当前主路线模型列表中选择一个可选的“轻量任务模型”，完整接管备用路线原先承担的所有功能。

## 已确认原因

### 1. 桌面端把流式响应缓存成了完整响应

本地运行时和 API 客户端已经支持 NDJSON 流式事件，但桌面主进程的运行时代理会先执行 `response.arrayBuffer()`，等待整个请求结束后再通过 IPC 返回。前端虽然按流读取，实际拿到的仍是一次性完整数据。

另外，当前会话生成路径主要使用 `streamCompletion()`，只转发回答正文。模型返回的 reasoning/thinking 数据没有稳定地作为独立流式通道传到界面。

### 2. 工作流默认继承 AI 助手当前会话

`runWorkflowSkill()`、拆书附件上传、拆书归档和部分蒸馏操作默认使用 `conversationDetail?.id` 或 `ensureConversationId()`。这些方法指向当前 AI 助手会话，因此任务消息、附件、摘要和技能状态可能进入普通对话。

### 3. 状态提示被绝对定位到发送按钮旁边

状态提示当前放在 `.assistant-composer-actions` 中，并通过 `right: 42px`、`bottom: 7px` 绝对定位。它会挤在模型选择和发送按钮附近，与截图中工具栏中部的目标位置不一致，也容易在窄窗口中遮挡操作。

### 4. “添加规则”没有按当前分类提供可编辑区域

“添加规则”按钮确实绑定了点击事件，但当前处于“范文片段”分类时，点击只会在内存中新增 `style_example` 并切换选中项。中间区域仍固定渲染 `style_rule` 列表，没有渲染新增范文的编辑表单，所以用户看不到新增结果，表现为点击无响应。

同一页面还存在动作名称与当前分类不一致的问题。“范文片段”和“禁用表达”仍显示“添加规则”，无法准确说明按钮将创建什么。

### 5. AI 写入的文件不是页面读取的结构化主数据

已检查当前项目文件，确认这不是单纯的页面刷新问题，而是写入链路分裂：

- AI 执行“更新一下题材库”时创建了 `00_设定集/题材库/更新请求.txt`，该文件只是普通文本，并且内容仍是“请提供具体更新内容”的请求占位文本。
- “风格与题材”页面读取的是 `00_设定集/.agent/libraries/style.v1.jsonl` 和 `genre.v1.jsonl`。当前题材主数据只有元信息，没有任何题材记录，因此页面显示为空是符合现有数据的。
- AI 执行“将文件作为大纲写入”时保存到了 `01_大纲/大纲.txt`，但“故事大纲”页面读取的是 `00_设定集/.agent/story-planning.jsonl`，并由它生成 `01_大纲/故事大纲.md`。
- 当前结构化故事大纲仍是“新大纲事件”等占位节点，实际写入 `大纲.txt` 的完整大纲没有进入页面主数据。
- “设定资料”与风格、题材使用同一套结构化资料库机制；AI 如果只创建或修改设定目录中的普通文本，同样不会更新页面读取的 `lore.v1.jsonl`。

现有“完成”回执只代表 `create_file` 等文件操作执行成功，不代表故事大纲、风格库、题材库或设定资料已经完成结构化提交。AI 可以绕过 `ProjectLibraryService` 和 `StoryPlanningService` 写入兼容目录，造成“系统提示成功、页面没有内容”的假成功。

### 6. 分卷只有读取和展示，没有创建及管理入口

故事大纲页面已经识别 `kind: "volume"` 的结构化节点，也会把已有分卷列在左侧，但当前实现仅执行筛选和展示：

- 空状态“尚未建立分卷”只是静态文字，没有按钮或点击事件。
- 顶部只有主线、人物线、章节三个视图，没有分卷视图。
- “添加情节点”只会创建 `main_arc`、`character_arc` 或 `chapter`，不会创建 `volume`。
- 页面没有分卷重命名、目标编辑、排序、删除及章节归属操作。
- 当前没有分卷相关的组件测试或流程测试。

因此数据结构虽然支持分卷，用户界面却无法产生或维护分卷节点；只有外部数据或 AI 恰好写入 `volume` 节点时才可能显示。

### 7. 长任务没有任务级状态，拆书取消后无法定位或恢复

已确认当前拆书和批量章节生成都通过通用 `runWorkflowSkill()` 启动，但页面没有绑定独立任务状态：

- 拆书按钮点击后只清空全局提示并等待技能流，页面没有立即进入“准备拆解”状态。
- 拆书页只在右侧面板底部显示全局 `operationsMessage`，提示距离顶部“一键拆解”按钮和当前书籍状态较远，容易被忽略。
- 章节批量生成页面不渲染 `operationsMessage`，点击后除了按钮可能变为禁用，没有可见反馈。
- 页面文案称批量生成会进入后台任务，但当前入口仍直接调用前台技能流，没有保存可供页面跟踪的 taskId。
- 拆书 manifest 已定义 `progress.stage`、`completed_chapters` 和 `total_chapters`，实际拆解过程只改 `status`，没有持续更新章节进度。
- 批量生成内部按章节保存 checkpoint，但没有向页面发送“当前第几章、已完成几章”的结构化进度事件。
- 所有工作流共用一个 `abortRef` 和 `operationsBusy`；启动其他工作流或停止响应可能取消当前拆书，并被统一翻译成缺少原因的“操作已取消”。

当前项目中已出现两条同名 `290140` 拆书记录，创建时间相隔约 10 秒；其中一条状态为 `failed`，错误为“操作已取消”，但进度仍是 `imported · 0/0`。这说明拆书任务既可能重复建书，也没有记录取消发生在哪个阶段。仅增加视觉进度条无法解决功能不可用，必须同时修正任务身份、状态持久化、取消归因和失败恢复。

### 8. 备用线路是第二套连接，而不是轻量任务模型

当前“备用线路”包含副 API Key、副 Base URL、副 temperature 和副 top_p，相关字段同时存在于设置页、共享配置 schema、配置服务、兼容配置视图和测试数据中。它不是主路线下的模型选择，而是一套可以指向其他服务商的完整连接。

已确认备用线路目前承担的直接功能包括：

- 使用模型生成对话摘要。
- 正文生成后的辅助一致性评分。
- 全书一致性检查。

未配置备用线路时，上述任务已经回退到当前主线路，并使用较低温度。因此替换方案不需要保留第二套鉴权和采样参数，只需要允许用户从当前主路线返回的模型列表中指定一个模型 ID。

此外，Embedding 配置还会在特定模型下隐式借用副 API Key。彻底移除备用线路时必须同步移除这条隐藏依赖，否则界面删除后运行时仍会继续读取旧副线路密钥。

## 修改一：实现真正的桌面端流式输出

### 桌面 IPC 流式传输

涉及文件：

- `apps/desktop-shell/src/shared/channels.ts`
- `apps/desktop-shell/src/main/main.ts`
- `apps/desktop-shell/src/preload/index.ts`
- `apps/workbench/src/lib/runtime.ts`

修改内容：

1. 保留现有 `runtimeRequest` 处理普通 JSON 和文件请求。
2. 新增专用流式请求通道，使用请求 ID 和 Electron MessagePort/IPC 事件传递：
   - 响应头已就绪
   - 数据块到达
   - 正常结束
   - 请求错误
   - 用户取消
3. 主进程直接读取本地运行时 `response.body`，每收到一个数据块就立即转发，不再调用 `response.arrayBuffer()`。
4. preload 将数据块封装成浏览器 `ReadableStream<Uint8Array>`。
5. `createDesktopRuntimeFetch()` 根据 `Accept: application/x-ndjson` 或 `text/event-stream` 自动使用流式通道，其他请求继续使用原有代理。
6. 将前端 `AbortSignal` 传递到主进程；停止生成、页面卸载或项目切换时，同时关闭 IPC 流和模型请求。
7. 本地运行时流式响应补充禁止缓存的响应头，并在发送首个事件前刷新响应头。

### 回答与思考双通道

涉及文件：

- `packages/model-client/src/openai-compatible.ts`
- `packages/agent-runtime/src/chat-runner.ts`
- `packages/agent-runtime/src/runtime.ts`
- `packages/shared/src/schemas/agent.ts`
- `packages/api-client/src/client.ts`
- `apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts`

修改内容：

1. 会话流使用 `streamDetailedCompletion()`，分别接收 `reasoning` 和 `answer`。
2. 流式事件统一携带 `channel`：
   - `reasoning`：模型思考内容
   - `answer`：最终回答内容
3. 前端收到每个数据块后立即更新当前助手消息，不等待 `final`。
4. 开启思考模式时实时展开“正在思考”；正文开始后状态改为“思考完成”。
5. 未开启思考模式时不显示思考区域，也不把思考内容拼入回答正文。
6. 正常完成、停止生成和异常中断时，都分别持久化已接收的回答与思考内容。
7. 如果供应商不支持流式响应，界面明确保持“正在生成”，完成后一次性显示；不伪装成逐字输出。

### 流式输出验收标准

- 模型返回第一段数据后，界面在请求结束前已经出现文字。
- 至少连续收到两个数据块时，消息内容长度会连续增长。
- 开启思考模式后，思考内容先于或伴随回答实时显示。
- 点击停止后不再接收新数据，已生成内容保留并标记为已停止。
- 浏览器开发模式和打包后的 Electron 版本行为一致。

## 修改二：拆书与正文续写使用独立任务线程

### 会话类型

为会话增加向后兼容的类型和任务元数据：

- `assistant`：普通 AI 助手对话
- `disassembly`：拆书任务
- `continuation`：正文续写或扩写任务
- `distillation`：风格蒸馏任务
- `fusion`：融梗任务

旧会话缺少类型时按 `assistant` 处理。任务元数据记录来源文件、来源书籍、目标文件、任务入口和创建时间。

### 统一创建任务线程

在工作台控制器中增加 `createTaskConversation()`：

1. 新建独立会话，不复用 `conversationDetail.id`。
2. 使用当前全局默认模型和思考设置。
3. 不继承 AI 助手消息、摘要、固定上下文和附件。
4. 只注入当前任务明确选择的原文、拆书产物、章节、大纲和设定文件。
5. 返回任务专用 `conversation_id`，后续运行、保存、取消和日志全部使用该 ID。

### 拆书任务隔离

涉及文件：

- `apps/workbench/src/features/product/disassembly/DisassemblyProductPage.tsx`
- `apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts`
- `packages/conversation-service/src/service.ts`
- `packages/agent-runtime/src/runtime.ts`

处理规则：

1. 导入作品时先创建该作品的拆书线程，附件上传到该线程。
2. “一键拆解”使用该作品的独立线程。
3. “继续拆解”继续使用同一本书的拆书线程，不进入 AI 助手对话。
4. 融梗和蒸馏分别创建自己的任务线程。
5. 列出拆书库等只读操作不创建会话，也不传入当前 AI 助手会话 ID。

线程标题示例：

- `拆书 · 《书名》`
- `融梗 · 三本参考作品`
- `蒸馏 · 《书名》`

### 正文续写隔离

涉及文件：

- `apps/workbench/src/features/product/editor/EditorProductPage.tsx`
- `apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts`

处理规则：

1. “续写当前段落”“扩写当前段落”、正文生成和批量正文生成均创建新的 `continuation` 线程。
2. 每次生成只注入当前章节、明确选择的参考文件及项目大纲/设定链路。
3. 正文侧栏普通聊天继续使用侧栏独立会话，但快捷生成操作不复用普通聊天历史。
4. 生成结果仍先进入预览，用户确认后才写入正文。
5. AI 助手当前对话的消息数量、摘要、附件和技能状态不得变化。

### 任务状态隔离

将当前共用的取消控制拆分为：

- AI 助手对话控制器
- 前台任务控制器
- 后台任务 ID 到控制器的映射

避免拆书、续写和 AI 助手之间互相停止、覆盖加载状态或抢占提示信息。

### 会话展示规则

1. AI 助手默认只显示 `assistant` 会话。
2. 任务线程在对应功能页或操作历史中查看。
3. 后续如需统一查看，可在会话列表增加“任务线程”筛选，不把任务线程混入普通对话历史。

### 线程隔离验收标准

- 执行拆书后，AI 助手当前会话 ID 和消息数量不变。
- 执行正文续写后，AI 助手当前会话没有新增技能消息或附件。
- 拆书线程只包含该书的原文和拆解上下文。
- 正文续写线程只包含本次章节及明确选择的项目参考资料。
- 停止一个任务不会停止正在生成的 AI 助手回复。

## 修改三：移动 AI 助手底部状态提示

涉及文件：

- `apps/workbench/src/features/product/assistant/AssistantComposer.tsx`
- `apps/workbench/src/styles/assistant.css`

修改内容：

1. 将状态提示从右侧操作区移到“上下文”与“模型选择/发送”之间的中部状态槽。
2. 工具栏改为稳定的三段布局：
   - 左侧：添加资料、上下文
   - 中间：会话状态提示
   - 右侧：模型、思考、发送/停止
3. 取消 `right: 42px` 的绝对定位，状态提示参与正常布局，不覆盖按钮。
4. 状态样式改为轻量文本或低强调度提示，不使用浮动卡片效果。
5. 保留 `role="status"` 和 `aria-live="polite"`。
6. 长提示单行省略，鼠标悬停可查看完整内容。
7. 窄窗口下允许中间状态槽换到工具栏上一行，但不能挤压发送按钮或造成文本重叠。

验收标准：

- “本会话已恢复跟随默认模型”等提示显示在截图红圈指向的中部空白区域。
- 状态提示不会遮挡模型选择、思考程度和发送按钮。
- 输入框高度变化、附件上传和窗口缩放时布局不跳动。
- 提示三秒后按现有规则消失。

## 修改四：修复“添加规则”点击无可见响应

涉及文件：

- `apps/workbench/src/features/product/style/StyleProductPage.tsx`
- 对应样式文件
- 风格与题材页面测试文件

### 按分类提供明确动作

页面右上角按钮根据当前分类显示：

- 写作风格：`添加写作规则`
- 题材规则：`添加题材规则`
- 范文片段：`添加范文`
- 禁用表达：`添加禁用表达`

### 按分类渲染正确编辑器

1. 写作风格和题材规则显示规则名称、规则内容、启用状态与删除操作。
2. 范文片段显示可编辑的名称、应用前、应用后、说明和来源字段。
3. 禁用表达显示原表达、替代建议、原因和删除操作。
4. 不再让“范文片段”分类继续显示“大纲叙事规则”区域。

### 点击反馈

1. 点击添加后立即把新条目插入当前分类。
2. 自动滚动到新条目并聚焦第一个输入框。
3. 显示“已添加，保存后生效”的非阻塞状态提示。
4. 新增内容进入未保存状态，离开页面或切换项目时走现有未保存保护。
5. 数据尚未加载完成时禁用按钮，并显示明确原因，避免静默 `return`。
6. 保存失败时保留本地编辑内容，并显示可重试错误。

验收标准：

- 四个分类下点击右上角添加按钮，均立即出现对应可编辑条目。
- 新增范文后，中间区域可直接编辑，右侧预览随输入同步更新。
- 点击按钮、按 Enter/Space 触发按钮以及连续添加多条均正常。
- 保存并重新打开页面后，新条目仍存在。
- 空库、加载失败和版本冲突状态下不会出现无反馈点击。

## 修改五：统一领域数据写入与真实成功回执

### 明确唯一主数据

页面展示和 AI 写入必须共用同一份主数据：

- 故事大纲：通过 `StoryPlanningService` 读写 `00_设定集/.agent/story-planning.jsonl`。
- 设定资料、写作风格和题材规则：通过 `ProjectLibraryService` 读写 `00_设定集/.agent/libraries/*.v1.jsonl`。
- `01_大纲/大纲.txt`、`01_大纲/故事大纲.md`、`00_设定集/风格库/*.txt` 和 `00_设定集/题材库/*.txt` 仅作为正文文件或兼容投影，不再被当作结构化页面保存成功的依据。

### 按用户意图路由写入

1. 用户要求更新故事大纲、增加情节、删除情节或把现有文件导入故事大纲时，必须生成 `story_planning_patch`，不能直接用普通文件操作代替。
2. 用户要求更新设定、风格或题材库时，必须生成 `library_draft`，并明确目标领域和记录类型。
3. 只有用户明确要求创建或修改某个普通文本文件时，才使用 `document_write`。
4. “更新一下题材库”这类缺少具体内容的指令应继续追问，不能创建 `更新请求.txt` 后报告题材库已更新；创建大纲、写入大纲或更新设定的明确指令则必须进入对应结构化预览。
5. 普通文件即使位于 `题材库`、`风格库` 或 `01_大纲` 目录，也不能自动冒充结构化领域数据。

### 统一预览与确认事务

领域写入统一使用可追踪事务，至少包含事务 ID、目标领域、基础版本、变更预览和提交状态：

- `story_planning_patch`：故事大纲节点的新增、修改、移动和删除。
- `library_draft`：设定、风格、题材及相关规则记录的新增、修改和删除。
- `document_write`：明确的普通文件写入，不触发结构化页面更新。

处理流程：

1. AI 生成变更后展示预览，列出将新增、修改和删除的记录。
2. 用户确认后，由服务端根据事务 ID 提交，不能依赖用户再发送一句自然语言“确认保存”来猜测目标。
3. 风格和题材提交必须通过 `ProjectLibraryService`，原子更新结构化主数据及其兼容投影。
4. 故事大纲提交必须通过 `StoryPlanningService`，原子更新 `story-planning.jsonl`、`故事大纲.md` 和 `故事时间线.md`。
5. 提交时检查基础版本；遇到版本冲突必须重新生成预览，不能覆盖较新的页面编辑结果。
6. 提交失败时保留草稿和预览，明确显示失败原因及重试入口。

### 成功回执与页面刷新

1. 只有领域服务提交成功并重新读取到新版本后，界面才显示系统级成功回执。
2. 成功回执包含目标、版本和变更数量，例如：`系统已写入 · 题材库 · revision 3 · 新增 6 条`。
3. 模型自然语言中的“已保存”“已完成”不能作为成功状态，也不能覆盖系统回执。
4. 普通文件写入成功时明确提示：`普通文件已创建，不会更新故事大纲、风格与题材或设定资料页面`。
5. 提交成功后立即刷新对应页面数据；当前已打开目标页面时，不要求用户切页或重启软件。
6. 同步失效项目上下文、检索索引和相关缓存，确保后续 AI 创作读取到最新大纲、风格和题材资料。
7. 若主数据写入成功但投影或索引更新失败，显示“部分完成”及可重试项，不能显示完整成功。

### 现有项目数据恢复

1. 检测到 `01_大纲/大纲.txt` 有完整内容、但结构化故事大纲只有占位节点时，提供“导入并合并到故事大纲”的预览入口。
2. 导入时解析为结构化节点，展示新增、覆盖和冲突项，用户确认后再提交，不自动覆盖现有节点。
3. 设定资料、风格库、题材库兼容文本与结构化主数据不一致时，提供一次性校对和迁移预览。
4. `更新请求.txt` 这类请求占位文件不作为题材内容导入，避免把操作指令污染项目资料。
5. 恢复完成后重新生成兼容投影，并标记旧的分裂数据已处理。

### 全软件同类链路检查

检查所有 AI 生成并保存领域数据的入口，包括故事大纲、伏笔与时间线、设定资料、风格库、题材库、拆书融梗和蒸馏产物：

- 页面有结构化服务时，AI 必须通过对应服务提交。
- 兼容文件只由领域服务生成，不能成为独立写入目标。
- 所有成功提示必须来源于服务端提交结果。
- 所有提交后都要验证页面重新读取的数据与写入结果一致。

### 领域写入验收标准

- 输入“更新一下题材库”但未提供更新内容时，AI 会追问，不创建 `更新请求.txt`，也不显示保存成功。
- 生成并确认风格、题材或设定内容后，结构化主数据包含对应记录，“风格与题材”或“设定资料”页面立即显示新内容。
- 将现有文件作为故事大纲导入并确认后，`story-planning.jsonl` 与投影同步更新，“故事大纲”页面立即显示真实节点。
- AI 直接创建普通文件时，系统不会显示“风格库、题材库或故事大纲已更新”。
- 成功回执中的版本和变更数量与领域服务实际返回结果一致。
- 对兼容投影的异常直接写入会进入差异校对状态，不会静默覆盖主数据或报告领域更新成功。

## 修改六：补齐分卷创建与管理功能

涉及文件：

- `apps/workbench/src/features/product/outline/OutlineProductPage.tsx`
- 故事大纲页面对应样式文件
- `packages/shared/src/schemas/story-planning.ts`
- `packages/document-service/src/story-planning-service.ts`
- 故事大纲页面与服务测试文件

### 可操作的分卷入口

1. 将“尚未建立分卷”改为带明确操作的空状态，提供“新建分卷”按钮。
2. 在分卷列表标题旁保留常驻的加号图标，已有分卷后仍可继续创建。
3. 点击后在左侧列表直接插入可命名的新分卷并聚焦输入框，不弹出额外模态框。
4. 新建分卷使用 `kind: "volume"`，通过现有 `StoryPlanningService` 保存到结构化主数据。
5. 保存中禁用重复提交；成功后自动选中新分卷并显示详情，失败则保留用户输入和重试入口。

### 分卷详情与编辑

选中分卷时，中栏和右侧详情切换为分卷上下文，至少支持：

- 分卷名称。
- 分卷目标和剧情摘要。
- 状态：计划中、进行中、已完成。
- 关联章节及本卷情节点。
- 重命名、保存和删除。

现有“本卷目标、核心冲突、情绪走向”不能再借用前三个普通情节点的摘要冒充分卷信息。若继续保留这三项，需要为分卷增加明确字段或结构化子记录，并通过 schema 和服务统一保存。

### 章节与情节点归属

1. 选中分卷后，中栏只显示该分卷下的章节和情节点，同时提供“全部大纲”返回入口。
2. 新建章节或情节点时，默认将 `parent_id` 指向当前分卷。
3. 允许把未分卷的章节或情节点移动到指定分卷，也允许移出分卷。
4. 分卷列表显示关联章节数量，空分卷明确显示“0 章”。
5. 移动和排序只在同类型、同父级范围内计算，不能因隐藏节点导致跨类型错位。

### 排序与删除保护

1. 分卷支持上移和下移，顺序写入结构化大纲并同步投影文件。
2. 删除空分卷时使用二次确认。
3. 删除非空分卷时明确列出包含的章节和情节点，默认只删除分卷并把子节点移到“未分卷”，不级联删除正文或大纲内容。
4. 如需连同子节点删除，必须使用独立的高风险操作并再次确认。
5. 版本冲突、投影漂移或保存失败时不关闭编辑状态，也不显示成功。

### AI 与分卷联动

1. AI 创建、拆分或调整分卷时生成 `story_planning_patch`，变更预览中单独列出分卷、章节归属和受影响情节点。
2. 用户确认后仍由 `StoryPlanningService` 提交，不能直接修改 `故事大纲.md` 或 `大纲.txt`。
3. AI 完善大纲时保留现有分卷 ID 和章节归属，除非预览明确提出调整。
4. 提交成功后立即刷新故事大纲页面，并自动选中新建或主要变更的分卷。

### 分卷功能验收标准

- 空项目点击“新建分卷”后立即出现可编辑分卷，保存并重启软件后仍存在。
- 可以连续创建、重命名、排序和删除多个分卷。
- 章节和情节点可以分配到分卷、跨分卷移动或恢复为未分卷。
- 选中分卷后只显示该卷内容，返回“全部大纲”后恢复完整视图。
- 删除非空分卷不会误删章节正文或其大纲节点。
- AI 创建分卷经过预览确认后，结构化主数据、投影文件和页面显示一致。
- 保存失败或版本冲突时保留编辑内容，并且不显示“分卷已创建”。

## 修改七：为拆书与章节批量生成建立可恢复的任务进度

涉及文件：

- `apps/workbench/src/features/product/disassembly/DisassemblyProductPage.tsx`
- `apps/workbench/src/features/product/batch/BatchProductPage.tsx`
- `apps/workbench/src/features/product/tasks/TasksProductPage.tsx`
- `apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts`
- `packages/shared/src/schemas/agent.ts`
- `packages/shared/src/schemas/job.ts`
- `packages/api-client/src/client.ts`
- `packages/agent-runtime/src/workflows/disassemble-book.ts`
- `packages/agent-runtime/src/workflows/disassemble-library.ts`
- `packages/agent-runtime/src/workflows/batch-generate.ts`
- `apps/desktop-shell/src/main/runtime/job-routes.ts`

### 统一长任务进度契约

拆书和批量章节生成不再依赖全局文字提示，统一返回并持久化任务对象：

~~~ts
type LongTaskProgress = {
  task_id: string;
  conversation_id: string;
  kind: "disassembly" | "batch_generation";
  entity_id: string;
  status: "queued" | "running" | "paused" | "waiting_confirmation" | "done" | "failed" | "cancelled";
  stage: string;
  completed_units: number;
  total_units: number;
  percent: number | null;
  current_unit: string;
  message: string;
  started_at: string;
  updated_at: string;
  error?: { code: string; stage: string; detail: string; suggestion: string };
};
~~~

规则：

1. 点击后先创建任务并立即返回 taskId，页面在模型请求开始前就能显示“正在准备”。
2. 任务按 taskId 独立更新，不能再只使用全局 `operationsBusy`、`operationsMessage` 和 `abortRef`。
3. 进度基于真实完成单元计算；总量尚未确定时使用不定进度条，不伪造百分比。
4. 每个 checkpoint 同时更新任务进度，刷新页面或重启软件后可从持久化状态恢复。
5. 页面优先订阅任务事件；事件流中断时每秒轮询作为回退，恢复连接后继续使用同一个 taskId。
6. 暂停、继续、取消和重试均携带 taskId 与版本号，避免旧操作控制到新任务。

### 修复拆书任务可用性

一键拆解启动前执行明确的预检查：

- 已选择书籍且 source_book_id 存在。
- 原文路径存在、可读，字符数和 hash 与 manifest 一致。
- 原文不是空文件，编码异常时先提示并允许选择正确编码重新导入。
- 模型配置和授权可用，并明确显示将使用的模型。
- 同一本书没有另一个正在运行的拆书任务。

处理规则：

1. `source_book_id` 是拆解的唯一书籍标识；开始、失败重试和继续拆解都原地更新同一本书，禁止再次创建 bookId。
2. 点击后立即把该书状态改为 `queued`，任务真正开始后改为 `analyzing`。
3. 将拆解拆成可报告阶段：校验原文、建立章节索引、逐章分析、合并设定与事件、生成 Markdown 报告、读回校验、完成入库。
4. 章节分析阶段持续写入 `completed_chapters / total_chapters`；其他阶段显示阶段进度和不定进度条。
5. 取消不是失败：用户主动取消记录为 `cancelled` 并保留 checkpoint；“继续拆解”从最后成功阶段恢复。
6. 区分 `USER_CANCELLED`、`SUPERSEDED_BY_NEW_TASK`、`PROJECT_SWITCHED`、`SOURCE_MISSING`、`MODEL_CONFIG_INVALID`、`PROVIDER_ERROR` 和 `PROCESS_INTERRUPTED`，不再全部显示“操作已取消”。
7. 失败记录显示准确阶段、原因、建议和“从此阶段重试”；原文及已完成产物继续保留。
8. 对当前项目的重复 `290140` 记录提供合并预览：按 source hash 识别同一本书，保留原文最完整的记录和有效 checkpoint，不自动删除用户文件。

### 拆书工作台进度界面

1. 点击“一键拆解”后按钮立即变为“正在拆解”，并提供停止按钮。
2. 在当前书籍标题下方显示单一任务进度区，不把关键信息藏在右侧迁移面板底部。
3. 进度区包含阶段名称、进度条、已完成章节/总章节、当前处理章节、耗时和最近更新时间。
4. 左侧书籍条目同步显示简短状态，例如“分析 126/489 章”“生成报告”“等待继续”。
5. 用户切换书籍时展示各自任务状态；离开页面后任务继续，返回时自动恢复。
6. 完成后进度区切换为报告摘要和“打开报告”；失败或取消后原位置显示重试/继续操作。
7. 使用 `role="progressbar"`、`aria-valuemin`、`aria-valuemax` 和 `aria-valuenow`；阶段变化通过 `aria-live="polite"` 播报，不逐 token 播报。

### 章节批量生成进度界面

1. 点击“开始生成”后立即显示任务已创建、章节范围和 taskId，并把按钮改为暂停/取消操作。
2. 页面顶部或主内容区显示整体进度条：`已完成章节数 / 总章节数`，同时显示当前章节和当前阶段。
3. 章节列表为每章显示：等待、正在生成、正在一致性检查、等待确认、草稿可用、失败或已跳过。
4. 开启“每章生成后暂停”时，任务进入 `waiting_confirmation`，页面直接显示预览与“确认本章 / 重新生成 / 停止批次”，而不是只提示去后台任务。
5. 每章完成后更新已用字数、模型费用和剩余预算；达到预算上限时状态为“已暂停”，不能显示失败或完成。
6. 页面切换和应用重启后恢复当前批次；历史批次读取真实批量任务，不再混用无关 `snapshot.jobs`。
7. 某章失败时默认暂停在该章，保留此前草稿；允许重试本章、跳过本章或停止整个批次。
8. 只有草稿读回校验成功后，该章才计入完成数量；最终完成数必须与可打开的草稿数量一致。

### 与任务线程和真流式传输衔接

1. 拆书和批量生成任务使用“修改二”定义的独立任务线程，不写入普通 AI 助手会话。
2. 任务进度事件与模型正文流分开：进度事件更新任务状态，正文/思考增量只更新当前步骤的预览。
3. Electron 中通过“修改一”的分块 IPC 实时传递任务事件；不能等完整请求结束后再一次性跳到 100%。
4. 普通 AI 助手、拆书任务和批量生成任务分别使用取消控制器，互相不会触发“操作已取消”。

### 长任务验收标准

- 点击一键拆解或开始批量生成后，界面无需等待模型返回即可出现任务状态。
- 使用延迟模拟任务时，进度至少经过两个真实中间值后才到达 100%。
- 拆书按真实章节数显示进度，完成后报告可打开，刷新和重启后状态不丢失。
- 同一本书连续点击不会创建第二个 bookId 或第二个并行任务。
- 主动取消后显示“已取消”及继续入口，不显示“拆解失败”；继续后从 checkpoint 恢复。
- 模型配置、原文、编码或授权有问题时，开始前给出具体原因和处理建议。
- 批量生成逐章更新状态，暂停确认、单章重试、跳过和预算暂停均可用。
- 运行拆书时发送普通 AI 消息，或运行两个不同任务，不会互相取消或覆盖进度。

## 修改八：移除备用路线并增加轻量任务模型

涉及文件：

- `apps/workbench/src/features/settings/SettingsFeaturePage.tsx`
- `apps/workbench/src/views/ConfigView.tsx`
- `apps/workbench/src/lib/config.ts`
- `apps/workbench/src/features/product/assistant/AssistantModelControls.tsx`
- `apps/workbench/src/hooks/controllers/useWorkbenchCoreController.ts`
- `packages/shared/src/schemas/config.ts`
- `packages/config-service/src/service.ts`
- `packages/agent-runtime/src/workflows/body-generate.ts`
- `packages/agent-runtime/src/workflows/consistency-check.ts`
- `apps/desktop-shell/src/main/runtime/conversation-routes.ts`
- 相关配置、模型路由与界面测试文件

### 配置模型改造

1. 从前端、共享 schema、配置服务和运行时中移除：
   - `secondary_api_key`
   - `secondary_base_url`
   - `secondary_model`
   - `secondary_temp`
   - `secondary_top_p`
2. 在当前活动 AI 配置档案中只新增 `task_model: string`；手动配置和网站配置各自保存自己的轻量任务模型选择。
3. `task_model` 为空表示“跟随当前模型”，不增加额外开关。
4. 轻量任务模型始终继承当前主路线的 API Key、Base URL、授权状态、超时、重试和兼容协议，只覆盖模型 ID。
5. temperature、top_p 由具体轻量任务按现有业务语义设置；不再提供第二组全局滑杆。
6. 轻量任务模型不会修改 AI 助手当前会话模型，也不会成为新的全局默认模型。

### 设置页交互

彻底删除截图中的“备用线路”表单，原位置替换为紧凑的“轻量任务模型”区域：

- 一个模型选择控件。
- 一个刷新模型列表的图标按钮。
- 当前来源说明，例如“模型来自当前主路线”。
- 一项“跟随当前模型（默认）”。
- 简短用途说明：用于对话摘要、正文辅助检查和一致性检查。

模型列表规则：

1. 手动模式使用当前主线路 Base URL 与 API Key 调用现有模型发现接口。
2. 网站模式使用当前网站主路线返回的模型列表。
3. 只显示可选择且支持文本生成的模型。
4. 首次展开但尚未加载时显示加载状态；失败时保留当前选择并提供重试，不显示空白下拉框。
5. 切换主路线、配置模式或 API 地址后重新校验选择；原模型不存在时显示“当前路线不可用，暂时跟随当前模型”，不会继续请求旧接口。
6. 选择成功后显示“轻量任务使用：模型名”；选择“跟随当前模型”后立即清空 `task_model`。

### 统一轻量任务模型解析

配置服务新增唯一入口 `loadTaskModelConfig()`：

1. 先读取当前活动主路线配置。
2. `task_model` 有效时，复用主路线连接并将模型替换为该值。
3. `task_model` 为空或不在当前主路线模型目录中时，返回当前主模型。
4. 返回模型来源：`task-model`、`current-model-fallback` 或 `unavailable`，供界面、日志和错误文案使用。
5. 所有原先调用 secondary slot 的功能统一改用该入口，禁止各功能自行判断旧副线路字段。

迁移的功能范围：

- 对话摘要。
- 正文生成后的辅助一致性评分。
- 全书一致性检查。
- 配置健康检查中对备用模型可用性的判断和提示。
- 所有直接或间接调用 `loadModelConfig(..., "secondary")`、读取 `secondary_model` 或根据副线路是否存在进行分支的功能。
- 后续新增的摘要、分类、提取、校验和低成本辅助任务。

相应文案从“副线路未配置，已由主线路辅助代理完成”改为“未选择轻量任务模型，已使用当前模型”。任务结果和诊断信息记录实际模型 ID 与来源，但不记录 API Key。

### 备用路线功能完整继承规则

1. 建立旧功能到新解析入口的迁移清单，逐个删除旧分支；不能只修改当前已知的三个调用文件。
2. 仓库内任何运行时代码都不得继续读取 `secondary_*` 字段，也不得继续接受 `"secondary"` 模型槽位。
3. 原先“优先备用路线，未配置时回退主路线”的行为统一改为“优先轻量任务模型，未选择或不可用时使用当前主模型”。
4. 原先由备用路线执行的提示词、上下文、超时、重试、结构化输出解析、取消、日志和错误处理全部保持，不因更换模型来源而降级。
5. 每个迁移任务都记录 `requested_model`、`effective_model` 和 `model_source`，用于验证它确实由轻量任务模型接管。
6. 新增轻量任务时只能声明任务用途并调用 `loadTaskModelConfig()`，不得重新引入第二套连接或自行复制回退逻辑。
7. 配置健康状态从“副线路模型”改为“轻量任务模型”，显示“跟随当前模型”“已选择模型”或“所选模型在当前路线不可用”。
8. 备用路线原有的独立 API 故障切换能力不保留。轻量任务模型与当前主模型共用同一 API；主路线连接不可用时应显示真实连接错误，不能宣称已切换备用接口。

### 旧配置清理与迁移

1. 升级时读取一次旧 `secondary_model`；只有它存在于当前主路线模型列表时，才迁移为 `task_model`。
2. 旧备用模型不属于当前主路线时不迁移，`task_model` 保持为空并沿用当前模型。
3. 无论是否迁移成功，都从顶层配置、`manual_profile` 和 `website_profile` 中删除全部 `secondary_*` 字段。
4. 配置保存、导出和诊断接口不再返回旧字段；运行时不得再读取旧副 API Key 或 Base URL。
5. Embedding 未单独填写 API Key 时只允许按现有规则使用当前主路线 Key，不再读取 `secondary_api_key`。
6. 测试配置、示例配置和 E2E 固件同步删除旧副线路密钥，避免旧字段被重新写回。

### 轻量任务模型验收标准

- 设置页、兼容配置页和其他入口均不再出现备用线路、副 API Key、副 Base URL或第二组采样参数。
- 轻量任务模型列表只来自当前主路线 API，切换配置模式后列表与选择同步更新。
- 选择轻量任务模型后，对话摘要、正文辅助检查和一致性检查使用该模型，但普通 AI 助手模型保持不变。
- 不选择轻量任务模型时，上述功能继续使用当前主模型，行为不低于现有回退模式。
- 备用路线曾承担的每一项任务都有迁移测试，提示词、上下文、解析、重试、取消和结果格式保持一致。
- 全仓扫描不存在运行时 `secondary_*` 读取、`secondary` 模型槽位或绕过 `loadTaskModelConfig()` 的旧功能分支。
- 当前主路线模型列表不包含已保存选择时，系统明确回退当前模型，不发起无效模型请求。
- 保存配置后，配置文件及公开配置响应中不存在任何 `secondary_*` 字段或旧副线路密钥。
- 抓取请求记录时，轻量任务始终访问当前主路线 Base URL，不访问已删除的旧备用地址。

## 测试计划

### 单元测试

- 桌面流式消息通道的开始、分块、结束、异常和取消。
- NDJSON 跨数据块、半行和多行解析。
- reasoning 与 answer 双通道累积和持久化。
- `createTaskConversation()` 不继承普通助手上下文。
- 风格与题材四种分类的新增记录类型和默认值。
- 领域意图正确路由到 `story_planning_patch`、`library_draft` 或 `document_write`。
- 只有结构化服务提交并复读成功后才生成成功回执。
- 占位请求文件不会被识别为题材或风格内容。
- 分卷创建的节点类型、父子关系和同级排序正确。
- 删除非空分卷时子节点转为未分卷，正文文件不受影响。
- 长任务进度的状态转换、百分比边界、版本冲突和错误码映射正确。
- 拆书 checkpoint 更新 manifest 进度，批量生成 checkpoint 更新已完成章节数。
- 用户取消、项目切换、进程中断和新任务替代具有不同的取消原因。
- `loadTaskModelConfig()` 在已选择、未选择、选择失效和主路线不可用时返回正确模型与来源。
- 旧 `secondary_*` 配置迁移后只保留可验证的 `task_model`，并清除旧连接和密钥字段。
- 轻量任务参数不会覆盖主路线配置或 AI 助手会话偏好。

### 集成测试

- 模型模拟服务延迟发送至少三个数据块，断言首块在结束前到达渲染进程。
- 拆书、继续拆解、融梗、蒸馏分别使用预期任务线程。
- 正文续写生成后，AI 助手主会话保持不变。
- 状态提示在桌面宽度和窄窗口下均位于预期区域。
- 故事大纲、风格、题材和设定资料提交后，主数据、兼容投影与页面内容一致。
- 模拟主数据版本冲突、投影失败和索引失败，验证不会出现假成功。
- 现有 `大纲.txt` 通过恢复预览导入后，故事大纲页面显示导入节点。
- 分卷的新建、编辑、排序、章节分配和删除均通过页面与服务集成测试。
- 拆书从预检查、逐章分析、报告生成到读回校验持续产生可恢复进度。
- 批量生成逐章更新状态，并覆盖等待确认、预算暂停、单章失败重试和跳过。
- 同一本书重复点击一键拆解只返回已有活动 taskId，不产生重复书籍或并行任务。
- 对话摘要、正文辅助检查和一致性检查均通过统一轻量任务模型解析入口。
- 手动模式与网站模式的轻量模型列表分别来自各自当前主路线，切换后不会使用旧路线模型。
- 配置保存、重新加载和公开配置响应全链路不再出现 `secondary_*` 字段。

### Electron 烟测

1. 在打包桌面壳中发送 AI 消息，记录每个数据块的到达时间。
2. 开启思考模式，确认思考和回答分别实时显示。
3. 生成中点击停止，确认请求被取消且界面可再次发送。
4. 依次执行拆书和正文续写，检查产生不同的任务会话 ID。
5. 在“范文片段”分类点击“添加范文”，编辑并保存后重新加载验证持久化。
6. 在 AI 助手中分别创建或写入故事大纲，并更新题材库、风格库和设定资料，确认预览、提交、成功回执及页面刷新完整闭环。
7. 故意取消预览或制造写入失败，确认页面不变化且界面不显示“已完成更新”。
8. 从空大纲新建两个分卷，分配章节、调整顺序、删除一个非空分卷并重启软件，验证数据完整性。
9. 对含多章的测试小说执行拆解，确认点击后立即显示状态、进度持续变化、完成后报告可打开。
10. 执行三章批量生成，确认章节状态逐章变化，暂停、恢复和重启软件后任务仍可继续。
11. 拆书运行中发送普通 AI 消息并启动另一任务，确认彼此不会被取消或覆盖状态。
12. 在当前主路线选择轻量任务模型，分别执行对话摘要和一致性检查，核对实际请求模型与主路线地址。
13. 清空轻量任务模型后重复测试，确认自动使用当前主模型；切换主路线后确认无效旧选择得到明确回退提示。

## 实施顺序

1. 先补测试用的分段模型响应和任务会话断言，固定当前失败现象。
2. 改造桌面 IPC 流式通道并接通前端 `ReadableStream`。
3. 接通回答/思考双通道并验证停止生成。
4. 增加 `task_model` 和统一解析入口，迁移摘要与一致性检查调用。
5. 删除备用线路 UI、配置字段、运行时依赖和旧密钥，并完成配置迁移测试。
6. 增加任务会话类型和 `createTaskConversation()`。
7. 迁移拆书、续写、融梗和蒸馏入口，移除对当前 AI 助手会话的隐式依赖。
8. 建立可持久化的长任务进度契约，拆分任务状态和取消控制器。
9. 修复拆书预检查、稳定 bookId、阶段 checkpoint、取消归因和失败恢复。
10. 接入拆书工作台与章节批量生成的提示、进度条和任务控制。
11. 统一领域意图路由、预览事务、结构化提交和系统成功回执。
12. 增加现有分裂数据的恢复预览，验证大纲、风格和题材迁移。
13. 移动底部状态提示并完成响应式布局。
14. 重构风格与题材分类内容，修复新增、编辑、保存及反馈链路。
15. 补齐分卷创建、详情编辑、排序、归属和删除保护。
16. 运行单元测试、集成测试、Electron 烟测和桌面截图检查。

## 完成定义

只有同时满足以下条件才视为完成：

- Electron 正式构建中可以观察到逐段流式输出。
- 思考模式实时显示模型返回的思考通道。
- 拆书和正文续写不读取或修改 AI 助手当前会话历史。
- 状态提示稳定显示在输入框工具栏中部。
- “风格与题材”四个分类的添加按钮均有即时、可编辑、可保存的结果。
- AI 创建或写入故事大纲，以及更新风格、题材或设定资料后，结构化主数据、兼容投影和对应页面保持一致。
- 普通文件写入、取消提交和部分失败均不会显示领域数据已成功更新。
- 当前项目已有的完整 `大纲.txt` 可以通过预览安全导入故事大纲页面。
- 分卷可以在页面中创建、编辑、排序、分配章节和安全删除，不再只有静态提示。
- 一键拆解和章节批量生成点击后立即反馈，持续显示真实进度，并支持暂停、取消、继续和失败重试。
- 拆书不再因共享取消状态而无故变成“操作已取消”，同一本书不会产生重复记录。
- 长任务切换页面或重启软件后仍可恢复，完成数量与可读取产物一致。
- 备用线路及全部 `secondary_*` 配置和运行时依赖已移除，设置页只保留从当前主路线选择的轻量任务模型。
- 选择轻量任务模型时原备用路线功能完整迁移；未选择时继续使用当前主模型，不改变 AI 助手模型选择。
- 新增测试全部通过，并完成至少一次真实桌面烟测。
